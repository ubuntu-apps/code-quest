import { makeTopic, ch, qMcq, qShort, pyOut, pyVar, regex, includes } from './_builders.mjs'
import { helpFor } from './control-flow-help.mjs'

function chHelp(topicId, index, difficulty, title, prompt, validation, extra = {}) {
  return ch(difficulty, title, prompt, validation, { ...extra, ...helpFor(topicId, index) })
}

export const controlFlowTopics = [
  makeTopic({
    id: 'if_elif_else',
    title: 'if elif else',
    paragraphs: [
      'Conditional statements let your program choose different paths. `if condition:` runs a block when the condition is true; `elif` checks another condition; `else` runs when none matched.',
      'Indentation defines which lines belong to each branch. Conditions are expressions that evaluate to True or False.',
      'Use if/elif/else to handle user choices, validate data, and implement game rules.',
    ],
    sandboxCode: 'score = 85\nif score >= 90:\n    print("A")\nelif score >= 80:\n    print("B")\nelse:\n    print("C")',
    challenges: [
      chHelp('if_elif_else', 0, 'easy', 'Simple if', 'If `x > 0`, print `positive` (set x=5).', pyOut('positive'), { starterCode: 'x = 5\nif x > 0:\n    print(' }),
      chHelp('if_elif_else', 1, 'easy', 'else branch', 'Print `no` when `n=0`.', pyOut('no'), { starterCode: 'n = 0\nif n:\n    print("yes")\nelse:\n    print(' }),
      chHelp('if_elif_else', 2, 'medium', 'elif chain', 'For `v=2`, print `two` using elif.', pyOut('two'), { starterCode: 'v = 2\nif v == 1:\n    print("one")\nelif v == 2:\n    print(' }),
      chHelp('if_elif_else', 3, 'medium', 'Compare strings', 'If name=="Ada", print `Hi Ada`.', pyOut('Hi Ada'), { starterCode: 'name = "Ada"\nif name == "Ada":\n    print(' }),
      chHelp('if_elif_else', 4, 'hard', 'Nested if', 'Set a=1,b=2; if a<b print `ok`.', pyOut('ok'), { starterCode: 'a,b=1,2\nif a<b:\n    print(' }),
      chHelp('if_elif_else', 5, 'hard', 'Grade logic', 'score=74 → print `Pass` if >=70 else `Fail`.', pyOut('Pass'), { starterCode: 'score = 74\n' }),
      chHelp('if_elif_else', 6, 'expert', 'Multiple elif', 'Map 1→one, 2→two, else other for x=3.', pyOut('other'), { starterCode: 'x = 3\n' }),
      chHelp('if_elif_else', 7, 'expert', 'Truthy check', 'If lst: print `has items` for lst=[1].', pyOut('has items'), { starterCode: 'lst = [1]\n' }),
      chHelp('if_elif_else', 8, 'expert', 'if not', 'Print `empty` when s="" using if not.', pyOut('empty'), { starterCode: 's = ""\n' }),
      chHelp('if_elif_else', 9, 'expert', 'Combined condition', 'Print `teen` if 13<=age<=19, age=15.', pyOut('teen'), { starterCode: 'age = 15\n' }),
    ],
    questions: [
      qMcq('else runs when:', [{ id: 'a', label: 'No if/elif was true' }, { id: 'b', label: 'Always first' }, { id: 'c', label: 'Never' }], 'a'),
      qShort('Keyword for additional condition after if?', 'elif'),
      qMcq('if 0: print(1) prints:', [{ id: 'a', label: 'Nothing' }, { id: 'b', label: '1' }, { id: 'c', label: '0' }], 'a'),
      qShort('Colon after if condition is:', 'required'),
      qMcq('Indentation in if blocks is:', [{ id: 'a', label: 'Required' }, { id: 'b', label: 'Optional' }, { id: 'c', label: 'Forbidden' }], 'a'),
      qMcq('elif is short for:', [{ id: 'a', label: 'else if' }, { id: 'b', label: 'end if' }, { id: 'c', label: 'every if' }], 'a'),
      qShort('Boolean result of 5 > 3?', 'True'),
      qMcq('Expert: if [] is:', [{ id: 'a', label: 'Falsy' }, { id: 'b', label: 'Truthy' }, { id: 'c', label: 'Error' }], 'a'),
      qMcq('Expert: only one if/elif branch:', [{ id: 'a', label: 'Runs per chain' }, { id: 'b', label: 'All run' }, { id: 'c', label: 'Random' }], 'a'),
      qShort('Expert: not True equals?', 'False'),
    ],
  }),

  makeTopic({
    id: 'boolean_logic',
    title: 'Boolean Logic',
    paragraphs: [
      'Booleans represent truth: `True` and `False`. Combine conditions with `and`, `or`, and `not`. Short-circuit evaluation skips right side when result is already determined.',
      'Comparison operators produce booleans. Membership `in` and identity `is` also return True/False.',
      'Clear boolean expressions make conditions readable; avoid overly nested logic when elif chains are clearer.',
    ],
    sandboxCode: 'a, b = True, False\nprint(a and b, a or b, not a)\nprint(3 in [1,2,3])\nprint(None is None)',
    challenges: [
      chHelp('boolean_logic', 0, 'easy', 'and', 'Set r = True and False.', pyVar('r', 'r == False'), { starterCode: 'r = ' }),
      chHelp('boolean_logic', 1, 'easy', 'or', 'Set r = False or True.', pyVar('r', 'r == True'), { starterCode: 'r = ' }),
      chHelp('boolean_logic', 2, 'medium', 'not', 'Set r = not False.', pyVar('r', 'r == True'), { starterCode: 'r = ' }),
      chHelp('boolean_logic', 3, 'medium', 'in operator', 'Set ok = "a" in "cat".', pyVar('ok', 'ok == False'), { starterCode: 'ok = ' }),
      chHelp('boolean_logic', 4, 'hard', 'Chained and', '5>1 and 10>5 into flag.', pyVar('flag', 'flag == True'), { starterCode: 'flag = ' }),
      chHelp('boolean_logic', 5, 'hard', 'Mixed or/and', '(True or False) and True', pyVar('x', 'x == True'), { starterCode: 'x = ' }),
      chHelp('boolean_logic', 6, 'expert', 'De Morgan', 'not (a and b) with a=True,b=False.', pyVar('r', 'r == True'), { starterCode: 'a,b=True,False\nr=' }),
      chHelp('boolean_logic', 7, 'expert', 'is vs ==', '[] is [] is False; set x = ([] == [])', pyVar('x', 'x == True'), { starterCode: 'x = [] == []' }),
      chHelp('boolean_logic', 8, 'expert', 'any()', 'any([0,0,3]) into r.', pyVar('r', 'r == True'), { starterCode: 'r = any([0,0,3])' }),
      chHelp('boolean_logic', 9, 'expert', 'all()', 'all([1,2,3]) into r.', pyVar('r', 'r == True'), { starterCode: 'r = all([1,2,3])' }),
    ],
    questions: [
      qMcq('True and False is:', [{ id: 'a', label: 'False' }, { id: 'b', label: 'True' }, { id: 'c', label: 'None' }], 'a'),
      qShort('Operator for logical NOT?', 'not'),
      qMcq('True or False is:', [{ id: 'a', label: 'True' }, { id: 'b', label: 'False' }, { id: 'c', label: '0' }], 'a'),
      qShort('"x" in "box" is?', 'True'),
      qMcq('is checks:', [{ id: 'a', label: 'Identity (same object)' }, { id: 'b', label: 'Value equality only always' }, { id: 'c', label: 'Type only' }], 'a'),
      qMcq('any([]) is:', [{ id: 'a', label: 'False' }, { id: 'b', label: 'True' }, { id: 'c', label: '[]' }], 'a'),
      qShort('all([1,0]) is?', 'False'),
      qMcq('Expert: short-circuit means:', [{ id: 'a', label: 'Skip rest if result known' }, { id: 'b', label: 'Always evaluate all' }, { id: 'c', label: 'Compile error' }], 'a'),
      qMcq('Expert: bool(\"\") is:', [{ id: 'a', label: 'False' }, { id: 'b', label: 'True' }, { id: 'c', label: '""' }], 'a'),
      qShort('Expert: not (True or False) is?', 'False'),
    ],
  }),

  makeTopic({
    id: 'comparison_operators',
    title: 'Comparison Operators',
    paragraphs: [
      'Compare values with `==`, `!=`, `<`, `>`, `<=`, `>=`. They return True or False without modifying operands.',
      'Chained comparisons like `1 < x < 10` are idiomatic Python. Strings compare lexicographically; types must usually be compatible.',
      'Use comparisons in if statements, while loops, and sorting keys.',
    ],
    sandboxCode: 'print(3 == 3, 3 != 4)\nprint(2 < 5, 5 <= 5)\nprint("a" < "b")\nprint(1 < 2 < 3)',
    challenges: [
      chHelp('comparison_operators', 0, 'easy', 'Equality', 'Set eq = (5 == 5).', pyVar('eq', 'eq == True'), { starterCode: 'eq = ' }),
      chHelp('comparison_operators', 1, 'easy', 'Not equal', 'Set ne = (1 != 2).', pyVar('ne', 'ne == True'), { starterCode: 'ne = ' }),
      chHelp('comparison_operators', 2, 'medium', 'Less than', '3 < 10 into r.', pyVar('r', 'r == True'), { starterCode: 'r = 3 < 10' }),
      chHelp('comparison_operators', 3, 'medium', 'String compare', '"apple" < "banana" into r.', pyVar('r', 'r == True'), { starterCode: 'r = ' }),
      chHelp('comparison_operators', 4, 'hard', 'Chained', '1 < 2 < 3 into r.', pyVar('r', 'r == True'), { starterCode: 'r = 1 < 2 < 3' }),
      chHelp('comparison_operators', 5, 'hard', '>= check', '5 >= 5 into r.', pyVar('r', 'r == True'), { starterCode: 'r = 5 >= 5' }),
      chHelp('comparison_operators', 6, 'expert', 'Sort check', 'Given nums=[3,1,2], sorted(nums)==sorted(nums) into r.', pyVar('r', 'r == True'), { starterCode: 'nums=[3,1,2]\nr=' }),
      chHelp('comparison_operators', 7, 'expert', 'None comparison', 'None == None into r.', pyVar('r', 'r == True'), { starterCode: 'r = None == None' }),
      chHelp('comparison_operators', 8, 'expert', 'Float compare', '0.1 + 0.2 == 0.3 into r (likely False).', pyVar('r', 'r == False'), { starterCode: 'r = 0.1 + 0.2 == 0.3' }),
      chHelp('comparison_operators', 9, 'expert', 'min comparison', 'min(4,9)==4 into r.', pyVar('r', 'r == True'), { starterCode: 'r = min(4,9) == 4' }),
    ],
    questions: [
      qMcq('== tests:', [{ id: 'a', label: 'Equality' }, { id: 'b', label: 'Assignment' }, { id: 'c', label: 'Identity only' }], 'a'),
      qShort('Operator for not equal?', '!='),
      qMcq('1 < 2 < 3 is:', [{ id: 'a', label: 'True' }, { id: 'b', label: 'False' }, { id: 'c', label: 'SyntaxError' }], 'a'),
      qShort('>= means greater or:', 'equal'),
      qMcq('Comparing str uses:', [{ id: 'a', label: 'Lexicographic order' }, { id: 'b', label: 'Length only' }, { id: 'c', label: 'Random' }], 'a'),
      qMcq('3 > 3 is:', [{ id: 'a', label: 'False' }, { id: 'b', label: 'True' }, { id: 'c', label: '3' }], 'a'),
      qShort('Comparison result type?', 'bool'),
      qMcq('Expert: is not == for lists:', [{ id: 'a', label: 'Usually true for two []' }, { id: 'b', label: 'Always same' }, { id: 'c', label: 'Invalid' }], 'a'),
      qMcq('Expert: None == None:', [{ id: 'a', label: 'True' }, { id: 'b', label: 'False' }, { id: 'c', label: 'Error' }], 'a'),
      qShort('Expert: != is opposite of?', '=='),
    ],
  }),

  makeTopic({
    id: 'while_loops',
    title: 'while Loops',
    paragraphs: [
      'A `while condition:` loop repeats its body while the condition is true. Update variables inside the loop to avoid infinite loops.',
      'while suits unknown iteration counts — reading until sentinel, retrying input, or game loops.',
      'Always ensure the condition eventually becomes false, or use break to exit early.',
    ],
    sandboxCode: 'n = 3\nwhile n > 0:\n    print(n)\n    n -= 1\nprint("done")',
    challenges: [
      chHelp('while_loops', 0, 'easy', 'Count down', 'While n>0 print n; start n=2 (print 2\\n1).', pyOut('2\n1'), { starterCode: 'n = 2\nwhile n > 0:\n    print(n)\n    n -= 1' }),
      chHelp('while_loops', 1, 'easy', 'Sum loop', 'Sum 1..3 using while into total.', pyVar('total', 'total == 6'), { starterCode: 'total=0\ni=1\nwhile i<=3:\n total+=i\n i+=1' }),
      chHelp('while_loops', 2, 'medium', 'Double until', 'Start x=1; while x<10: x*=2; final x=8.', pyVar('x', 'x == 8'), { starterCode: 'x=1\nwhile x<10:\n x*=2' }),
      chHelp('while_loops', 3, 'medium', 'Counter', 'Count to 5 with while, store count in c.', pyVar('c', 'c == 5'), { starterCode: 'c=0\nwhile c<5:\n c+=1' }),
      chHelp('while_loops', 4, 'hard', 'Accumulate list', 'Build [0,1,2] with while append pattern.', pyVar('lst', 'lst == [0,1,2]'), { starterCode: 'lst=[]\ni=0\nwhile i<3:\n lst.append(i)\n i+=1' }),
      chHelp('while_loops', 5, 'hard', 'break when found', 'Loop until i==3 then break; set found=True.', pyVar('found', 'found == True'), { starterCode: 'i=0\nfound=False\nwhile True:\n i+=1\n if i==3:\n  found=True\n  break' }),
      chHelp('while_loops', 6, 'expert', 'Collatz step', 'While n!=1: if even n//=2 else n=3*n+1; start n=5 end 16?', pyVar('n', 'n == 16'), { starterCode: 'n=5\nwhile n!=1:\n n=n//2 if n%2==0 else 3*n+1' }),
      chHelp('while_loops', 7, 'expert', 'GCD while', 'Euclid: while b: a,b=b,a%b; start 12,8 → a=4.', pyVar('a', 'a == 4'), { starterCode: 'a,b=12,8\nwhile b:\n a,b=b,a%b' }),
      chHelp('while_loops', 8, 'expert', 'Infinite guard', 'Use while False: never run — set ran=0.', pyVar('ran', 'ran == 0'), { starterCode: 'ran=0\nwhile False:\n ran=1' }),
      chHelp('while_loops', 9, 'expert', 'Menu simulation', 'While choice!="q": one iteration; choice="q" after.', pyVar('choice', "choice == 'q'"), { starterCode: 'choice=""\nwhile choice!="q":\n choice="q"' }),
    ],
    questions: [
      qMcq('while loop runs while:', [{ id: 'a', label: 'Condition is True' }, { id: 'b', label: 'Condition is False' }, { id: 'c', label: 'Once only' }], 'a'),
      qShort('Keyword to exit loop immediately?', 'break'),
      qMcq('Infinite loop risk when:', [{ id: 'a', label: 'Condition never becomes false' }, { id: 'b', label: 'Using for' }, { id: 'c', label: 'Printing once' }], 'a'),
      qShort('while True: means condition always:', 'True'),
      qMcq('while suits:', [{ id: 'a', label: 'Unknown iterations' }, { id: 'b', label: 'Fixed sequence only' }, { id: 'c', label: 'No variables' }], 'a'),
      qMcq('i += 1 in while often:', [{ id: 'a', label: 'Updates counter' }, { id: 'b', label: 'Defines class' }, { id: 'c', label: 'Imports module' }], 'a'),
      qShort('else clause on while runs if loop ended without?', 'break'),
      qMcq('Expert: while n: with n=0 runs:', [{ id: 'a', label: 'Zero times' }, { id: 'b', label: 'Forever' }, { id: 'c', label: 'Once' }], 'a'),
      qMcq('Expert: continue in while:', [{ id: 'a', label: 'Skips to next iteration' }, { id: 'b', label: 'Exits program' }, { id: 'c', label: 'Restarts Python' }], 'a'),
      qShort('Expert: Sentinel value often ends:', 'input loops'),
    ],
  }),

  makeTopic({
    id: 'for_loops',
    title: 'for Loops',
    paragraphs: [
      '`for item in iterable:` visits each element in a sequence or collection. It is the preferred loop when you know what you are iterating over.',
      'Loop over strings, lists, ranges, dict keys, and file lines. The loop variable takes each value in turn.',
      'for loops are clearer than while for most collection processing and avoid manual index management.',
    ],
    sandboxCode: 'for ch in "hi":\n    print(ch)\nfor n in [10, 20, 30]:\n    print(n * 2)',
    challenges: [
      chHelp('for_loops', 0, 'easy', 'Loop list', 'Print each item of [1,2,3] on its own line.', pyOut('1\n2\n3'), { starterCode: 'for x in [1,2,3]:\n    print(x)' }),
      chHelp('for_loops', 1, 'easy', 'Loop string', 'Sum ord values? Set total=0; for c in "ab": total+=1 → 2.', pyVar('total', 'total == 2'), { starterCode: 'total=0\nfor c in "ab":\n total+=1' }),
      chHelp('for_loops', 2, 'medium', 'Accumulate', 'Sum [1,2,3,4] into s.', pyVar('s', 's == 10'), { starterCode: 's=0\nfor n in [1,2,3,4]:\n s+=n' }),
      chHelp('for_loops', 3, 'medium', 'Enumerate', 'for i,x in enumerate(["a","b"]): set last i=1.', pyVar('i', 'i == 1'), { starterCode: 'for i,x in enumerate(["a","b"]):\n pass' }),
      chHelp('for_loops', 4, 'hard', 'Filter build', 'Build evens from range(5) → [0,2,4].', pyVar('ev', 'ev == [0,2,4]'), { starterCode: 'ev=[]\nfor n in range(5):\n if n%2==0: ev.append(n)' }),
      chHelp('for_loops', 5, 'hard', 'Nested for', 'Pairs (0,0),(0,1) count = 4 for range(2) nested.', pyVar('c', 'c == 4'), { starterCode: 'c=0\nfor i in range(2):\n for j in range(2):\n  c+=1' }),
      chHelp('for_loops', 6, 'expert', 'zip loop', 'for a,b in zip([1,2],[3,4]): set s=a+b first → 4.', pyVar('s', 's == 4'), { starterCode: 'for a,b in zip([1,2],[3,4]):\n s=a+b\n break' }),
      chHelp('for_loops', 7, 'expert', 'else on for', 'for x in []: pass\nelse: done=True', pyVar('done', 'done == True'), { starterCode: 'done=False\nfor x in []:\n pass\nelse:\n done=True' }),
      chHelp('for_loops', 8, 'expert', 'Dict keys', 'for k in {"a":1}: collect k.', pyVar('k', "k == 'a'"), { starterCode: 'for k in {"a":1}:\n pass' }),
      chHelp('for_loops', 9, 'expert', 'Reverse iterate', 'for x in reversed([1,2,3]): first x=3.', pyVar('x', 'x == 3'), { starterCode: 'for x in reversed([1,2,3]):\n break' }),
    ],
    questions: [
      qMcq('for x in [1,2]: runs:', [{ id: 'a', label: '2 iterations' }, { id: 'b', label: '1 iteration' }, { id: 'c', label: 'Infinite' }], 'a'),
      qShort('Loop variable takes each:', 'item'),
      qMcq('for c in "ab": c visits:', [{ id: 'a', label: 'Characters' }, { id: 'b', label: 'Indexes only' }, { id: 'c', label: 'Integers' }], 'a'),
      qShort('enumerate provides index and:', 'value'),
      qMcq('zip pairs:', [{ id: 'a', label: 'Parallel iterables' }, { id: 'b', label: 'Files only' }, { id: 'c', label: 'Random numbers' }], 'a'),
      qMcq('for/else else runs when:', [{ id: 'a', label: 'No break occurred' }, { id: 'b', label: 'Always before loop' }, { id: 'c', label: 'Never' }], 'a'),
      qShort('reversed() returns:', 'iterator'),
      qMcq('Expert: iterating dict default keys:', [{ id: 'a', label: 'True in Py3' }, { id: 'b', label: 'Values only' }, { id: 'c', label: 'Error' }], 'a'),
      qMcq('Expert: for is preferred over while when:', [{ id: 'a', label: 'Iterating collections' }, { id: 'b', label: 'Never' }, { id: 'c', label: 'Writing binary' }], 'a'),
      qShort('Expert: break exits:', 'loop'),
    ],
  }),

  makeTopic({
    id: 'range_function',
    title: 'range()',
    paragraphs: [
      '`range(stop)` produces 0..stop-1. `range(start, stop)` and `range(start, stop, step)` control bounds and stride.',
      'range objects are lazy sequences — memory efficient for large spans. Convert with `list(range(5))` to see values.',
      'range is the standard partner to for loops for numeric iteration.',
    ],
    sandboxCode: 'print(list(range(5)))\nprint(list(range(2, 8, 2)))\nfor i in range(3):\n    print(i ** 2)',
    challenges: [
      chHelp('range_function', 0, 'easy', 'range(5)', 'list(range(5)) into r.', pyVar('r', 'r == [0,1,2,3,4]'), { starterCode: 'r = list(range(5))' }),
      chHelp('range_function', 1, 'easy', 'range start', 'list(range(2,5)) into r.', pyVar('r', 'r == [2,3,4]'), { starterCode: 'r = list(range(2,5))' }),
      chHelp('range_function', 2, 'medium', 'step', 'list(range(0,10,3)) into r.', pyVar('r', 'r == [0,3,6,9]'), { starterCode: 'r = list(range(0,10,3))' }),
      chHelp('range_function', 3, 'medium', 'Sum range', 'sum(range(1,6)) into s.', pyVar('s', 's == 15'), { starterCode: 's = sum(range(1,6))' }),
      chHelp('range_function', 4, 'hard', 'Reverse range', 'list(range(5,0,-1)) into r.', pyVar('r', 'r == [5,4,3,2,1]'), { starterCode: 'r = list(range(5,0,-1))' }),
      chHelp('range_function', 5, 'hard', 'len range', 'len(range(10)) into n.', pyVar('n', 'n == 10'), { starterCode: 'n = len(range(10))' }),
      chHelp('range_function', 6, 'expert', 'Empty range', 'list(range(3,3)) into r.', pyVar('r', 'r == []'), { starterCode: 'r = list(range(3,3))' }),
      chHelp('range_function', 7, 'expert', 'Negative step', 'list(range(10,0,-2)) into r.', pyVar('r', 'r == [10,8,6,4,2]'), { starterCode: 'r = list(range(10,0,-2))' }),
      chHelp('range_function', 8, 'expert', 'Index loop', 'Build squares 0,1,4 using range(3).', pyVar('sq', 'sq == [0,1,4]'), { starterCode: 'sq=[]\nfor i in range(3):\n sq.append(i*i)' }),
      chHelp('range_function', 9, 'expert', 'range vs list', 'type(range(3)) is range.', pyVar('t', "t.__name__ == 'range'"), { starterCode: 't = type(range(3))' }),
    ],
    questions: [
      qMcq('range(3) yields:', [{ id: 'a', label: '0,1,2' }, { id: 'b', label: '1,2,3' }, { id: 'c', label: '0,1,2,3' }], 'a'),
      qShort('range(2,5) stops before?', '5'),
      qMcq('Third argument to range is:', [{ id: 'a', label: 'step' }, { id: 'b', label: 'stop only' }, { id: 'c', label: 'type' }], 'a'),
      qShort('Convert range to list function?', 'list'),
      qMcq('range(5,5) is:', [{ id: 'a', label: 'Empty' }, { id: 'b', label: '[5]' }, { id: 'c', label: 'Error' }], 'a'),
      qMcq('range(-1, -5, -1) goes:', [{ id: 'a', label: 'Downward' }, { id: 'b', label: 'Upward only' }, { id: 'c', label: 'Random' }], 'a'),
      qShort('range is memory efficient because it is:', 'lazy'),
      qMcq('Expert: range(10) has len:', [{ id: 'a', label: '10' }, { id: 'b', label: '9' }, { id: 'c', label: '11' }], 'a'),
      qMcq('Expert: for i in range(n) runs n times when start is:', [{ id: 'a', label: '0 default' }, { id: 'b', label: '1 always' }, { id: 'c', label: 'n' }], 'a'),
      qShort('Expert: step 0 in range is:', 'error'),
    ],
  }),

  makeTopic({
    id: 'break_continue',
    title: 'break and continue',
    paragraphs: [
      '`break` exits the innermost loop immediately. `continue` skips the rest of the current iteration and jumps to the next.',
      'Use break when you found what you need; use continue to skip invalid items without nesting deep if blocks.',
      'break and continue apply to the nearest enclosing loop only, not outer nested loops.',
    ],
    sandboxCode: 'for n in range(10):\n    if n == 5:\n        break\n    if n % 2 == 0:\n        continue\n    print(n)  # 1, 3 only before break at 5',
    challenges: [
      chHelp('break_continue', 0, 'easy', 'break early', 'Loop 0..9 break at 3; last printed 2 or count iterations 3.', pyVar('c', 'c == 3'), { starterCode: 'c=0\nfor i in range(10):\n c+=1\n if i==3: break' }),
      chHelp('break_continue', 1, 'easy', 'continue skip', 'Sum 1..5 skip 3 → 11.', pyVar('s', 's == 11'), { starterCode: 's=0\nfor i in range(1,6):\n if i==3: continue\n s+=i' }),
      chHelp('break_continue', 2, 'medium', 'Find first', 'Find first >5 in [2,7,4] → 7.', pyVar('found', 'found == 7'), { starterCode: 'found=None\nfor x in [2,7,4]:\n if x>5:\n  found=x\n  break' }),
      chHelp('break_continue', 3, 'medium', 'Skip evens', 'Build odds 1..5 → [1,3,5].', pyVar('o', 'o == [1,3,5]'), { starterCode: 'o=[]\nfor i in range(1,6):\n if i%2==0: continue\n o.append(i)' }),
      chHelp('break_continue', 4, 'hard', 'while break', 'while True: break; set ok=True.', pyVar('ok', 'ok == True'), { starterCode: 'ok=False\nwhile True:\n ok=True\n break' }),
      chHelp('break_continue', 5, 'hard', 'Nested break', 'Break inner only; outer completes 2 times.', pyVar('outer', 'outer == 2'), { starterCode: 'outer=0\nfor i in range(2):\n outer+=1\n for j in range(5):\n  break' }),
      chHelp('break_continue', 6, 'expert', 'Prime check break', 'Is 17 prime? Set prime=True using trial division break.', pyVar('prime', 'prime == True'), { starterCode: 'n=17\nprime=True\nfor d in range(2,int(n**0.5)+1):\n if n%d==0:\n  prime=False\n  break' }),
      chHelp('break_continue', 7, 'expert', 'continue string', 'Count non-space in "a b" → 2.', pyVar('c', 'c == 2'), { starterCode: 'c=0\nfor ch in "a b":\n if ch==" ": continue\n c+=1' }),
      chHelp('break_continue', 8, 'expert', 'else after break', 'for with break skips else; without break runs else.', pyVar('ran', 'ran == True'), { starterCode: 'ran=False\nfor x in [1]:\n pass\nelse:\n ran=True' }),
      chHelp('break_continue', 9, 'expert', 'Search sentinel', 'Find index of 9 in list or -1.', pyVar('idx', 'idx == -1'), { starterCode: 'idx=-1\nfor i,x in enumerate([1,2,3]):\n if x==9:\n  idx=i\n  break' }),
    ],
    questions: [
      qMcq('break:', [{ id: 'a', label: 'Exits loop' }, { id: 'b', label: 'Skips one iteration' }, { id: 'c', label: 'Restarts program' }], 'a'),
      qShort('continue skips to:', 'next iteration'),
      qMcq('break in nested loop exits:', [{ id: 'a', label: 'Inner loop only' }, { id: 'b', label: 'All loops' }, { id: 'c', label: 'Function' }], 'a'),
      qShort('for/else else skipped when:', 'break'),
      qMcq('continue in while:', [{ id: 'a', label: 'Goes to condition check' }, { id: 'b', label: 'Exits while' }, { id: 'c', label: 'Raises error' }], 'a'),
      qMcq('Use break when:', [{ id: 'a', label: 'Found target' }, { id: 'b', label: 'Need skip one item' }, { id: 'c', label: 'Define function' }], 'a'),
      qShort('Keyword cannot exit function (use return):', 'break'),
      qMcq('Expert: while True: break runs:', [{ id: 'a', label: 'Once then exit' }, { id: 'b', label: 'Forever' }, { id: 'c', label: 'Never enters' }], 'a'),
      qMcq('Expert: continue avoids:', [{ id: 'a', label: 'Deep nesting for skip' }, { id: 'b', label: 'All loops' }, { id: 'c', label: 'Variables' }], 'a'),
      qShort('Expert: break in for else prevents:', 'else clause'),
    ],
  }),

  makeTopic({
    id: 'nested_loops',
    title: 'Nested Loops',
    paragraphs: [
      'Place one loop inside another to process grids, pairs, or combinations. Inner loop runs completely for each outer iteration.',
      'Common for multiplication tables, 2D lists, and comparing all pairs. Watch complexity — double loops over large data can be slow.',
      'Break inner loop only exits inner; use flags or functions to break outer from inner when needed.',
    ],
    sandboxCode: 'for row in range(3):\n    line = []\n    for col in range(3):\n        line.append(row * col)\n    print(line)',
    challenges: [
      chHelp('nested_loops', 0, 'easy', 'Simple nest', 'Count inner runs: outer 2, inner 3 → 6.', pyVar('c', 'c == 6'), { starterCode: 'c=0\nfor i in range(2):\n for j in range(3):\n  c+=1' }),
      chHelp('nested_loops', 1, 'easy', 'Print pairs', 'Print "0,0" and "0,1" using nested range(2).', pyOut('0,0\n0,1'), { starterCode: 'for i in range(2):\n for j in range(2):\n  if i==0: print(f"{i},{j}")' }),
      chHelp('nested_loops', 2, 'medium', 'Multiplication', 'Build row of 2*x for x in 0..3.', pyVar('row', 'row == [0,2,4,6]'), { starterCode: 'row=[]\nfor x in range(4):\n row.append(2*x)' }),
      chHelp('nested_loops', 3, 'medium', '2D sum', 'Sum [[1,2],[3,4]] nested.', pyVar('s', 's == 10'), { starterCode: 'm=[[1,2],[3,4]]\ns=0\nfor row in m:\n for v in row:\n  s+=v' }),
      chHelp('nested_loops', 4, 'hard', 'Triangle pattern', 'Print 3 lines "*" growing 1,2,3.', pyOut('*\n**\n***'), { starterCode: 'for i in range(1,4):\n print("*"*(i))' }),
      chHelp('nested_loops', 5, 'hard', 'Find pair sum', 'Find pair in [1,2,3] summing to 5.', pyVar('pair', 'pair == (2,3)'), { starterCode: 'pair=None\nnums=[1,2,3]\nfor i,a in enumerate(nums):\n for j,b in enumerate(nums):\n  if i<j and a+b==5: pair=(a,b)' }),
      chHelp('nested_loops', 6, 'expert', 'Matrix transpose', 'Transpose [[1,2],[3,4]] → [[1,3],[2,4]].', pyVar('t', 't == [[1,3],[2,4]]'), { starterCode: 'm=[[1,2],[3,4]]\nt=[[m[r][c] for r in range(2)] for c in range(2)]' }),
      chHelp('nested_loops', 7, 'expert', 'Bubble pass', 'One bubble pass on [3,1,2] → [1,3,2].', pyVar('a', 'a == [1,3,2]'), { starterCode: 'a=[3,1,2]\nfor i in range(len(a)-1):\n if a[i]>a[i+1]: a[i],a[i+1]=a[i+1],a[i]' }),
      chHelp('nested_loops', 8, 'expert', 'Cartesian count', '3x4 grid = 12 cells.', pyVar('n', 'n == 12'), { starterCode: 'n=0\nfor r in range(3):\n for c in range(4):\n  n+=1' }),
      chHelp('nested_loops', 9, 'expert', 'Break flag', 'Break outer from inner using flag.', pyVar('stopped', 'stopped == True'), { starterCode: 'stopped=False\nfor i in range(5):\n for j in range(5):\n  stopped=True\n  break\n if stopped: break' }),
    ],
    questions: [
      qMcq('Nested loops outer 2 inner 3 = iterations:', [{ id: 'a', label: '6' }, { id: 'b', label: '5' }, { id: 'c', label: '2' }], 'a'),
      qShort('Inner loop runs per outer:', 'iteration'),
      qMcq('2D list processing often uses:', [{ id: 'a', label: 'Nested loops' }, { id: 'b', label: 'import only' }, { id: 'c', label: 'No loops' }], 'a'),
      qShort('Break in inner exits:', 'inner'),
      qMcq('O(n²) often means:', [{ id: 'a', label: 'Nested loop over n' }, { id: 'b', label: 'Single pass' }, { id: 'c', label: 'Constant time' }], 'a'),
      qMcq('Grid rows and columns use:', [{ id: 'a', label: 'row loop + col loop' }, { id: 'b', label: 'one variable' }, { id: 'c', label: 'only while' }], 'a'),
      qShort('Flag variable helps break:', 'outer'),
      qMcq('Expert: transpose swaps:', [{ id: 'a', label: 'rows and columns' }, { id: 'b', label: 'only signs' }, { id: 'c', label: 'types' }], 'a'),
      qMcq('Expert: pair search uses:', [{ id: 'a', label: 'Two indices' }, { id: 'b', label: 'sort only' }, { id: 'c', label: 'print' }], 'a'),
      qShort('Expert: complexity grows when loops are:', 'nested'),
    ],
  }),

  makeTopic({
    id: 'match_case',
    title: 'match case',
    paragraphs: [
      'Python 3.10+ adds `match value:` with `case pattern:` branches — structural pattern matching similar to switch in other languages but more powerful.',
      'Match literals, capture variables, use `|` for alternatives, and `case _:` as wildcard default.',
      'Use match when you have many discrete cases on one value; if/elif remains fine for simple conditions.',
    ],
    sandboxCode: 'def http_status(n):\n    match n:\n        case 200: return "OK"\n        case 404: return "Not Found"\n        case _: return "Unknown"\nprint(http_status(404))',
    challenges: [
      chHelp('match_case', 0, 'easy', 'Literal case', 'match 1: case 1: x="one"', pyVar('x', "x == 'one'"), { starterCode: 'match 1:\n case 1:\n  x="one"' }),
      chHelp('match_case', 1, 'easy', 'Wildcard', 'match 0: case _: y="def"', pyVar('y', "y == 'def'"), { starterCode: 'match 0:\n case _:\n  y="def"' }),
      chHelp('match_case', 2, 'medium', 'String case', 'match "a": case "a"|"b": ok=True', pyVar('ok', 'ok == True'), { starterCode: 'match "a":\n case "a" | "b":\n  ok=True' }),
      chHelp('match_case', 3, 'medium', 'Capture', 'match [1,2]: case [x,y]: s=x+y', pyVar('s', 's == 3'), { starterCode: 'match [1,2]:\n case [x,y]:\n  s=x+y' }),
      chHelp('match_case', 4, 'hard', 'Guard', 'case n if n>0: pos=True for n=5', pyVar('pos', 'pos == True'), { starterCode: 'n=5\nmatch n:\n case x if x>0:\n  pos=True' }),
      chHelp('match_case', 5, 'hard', 'Multiple cases', 'match 3: case 1|2|3: hit=True', pyVar('hit', 'hit == True'), { starterCode: 'match 3:\n case 1|2|3:\n  hit=True' }),
      chHelp('match_case', 6, 'expert', 'Tuple pattern', 'match (1,"x"): case (1,s): v=s', pyVar('v', "v == 'x'"), { starterCode: 'match (1,"x"):\n case (1,s):\n  v=s' }),
      chHelp('match_case', 7, 'expert', 'Class pattern', 'Simple match on str type concept: isinstance via case _', pyVar('done', 'done == True'), { starterCode: 'done=False\nmatch "hi":\n case _:\n  done=True' }),
      chHelp('match_case', 8, 'expert', 'Default only', 'match None: case _: t="nil"', pyVar('t', "t == 'nil'"), { starterCode: 'match None:\n case _:\n  t="nil"' }),
      chHelp('match_case', 9, 'expert', 'List head', 'match [1,2,3]: case [h,*rest]: head=h', pyVar('head', 'head == 1'), { starterCode: 'match [1,2,3]:\n case [h, *rest]:\n  head=h' }),
    ],
    questions: [
      qMcq('match introduced in Python:', [{ id: 'a', label: '3.10+' }, { id: 'b', label: '2.7' }, { id: 'c', label: '3.0 only' }], 'a'),
      qShort('Wildcard case pattern?', '_'),
      qMcq('case _ means:', [{ id: 'a', label: 'Default catch-all' }, { id: 'b', label: 'Private variable' }, { id: 'c', label: 'Error' }], 'a'),
      qShort('Alternative cases use:', '|'),
      qMcq('case [x,y] is:', [{ id: 'a', label: 'Sequence pattern' }, { id: 'b', label: 'Function call' }, { id: 'c', label: 'Import' }], 'a'),
      qMcq('Guard uses:', [{ id: 'a', label: 'if condition on case' }, { id: 'b', label: 'only else' }, { id: 'c', label: 'try' }], 'a'),
      qShort('match replaces some:', 'if/elif chains'),
      qMcq('Expert: case 1 | 2 matches:', [{ id: 'a', label: '1 or 2' }, { id: 'b', label: '12' }, { id: 'c', label: 'nothing' }], 'a'),
      qMcq('Expert: match without case match:', [{ id: 'a', label: 'SyntaxError' }, { id: 'b', label: 'Runs all' }, { id: 'c', label: 'Returns None always' }], 'a'),
      qShort('Expert: *rest in pattern captures:', 'remaining'),
    ],
  }),

  makeTopic({
    id: 'loop_patterns',
    title: 'Loop Patterns and Algorithms',
    paragraphs: [
      'Common patterns: accumulate (sum/product), search (find first match), filter (build new list), and map (transform each item).',
      'Recognizing patterns helps you pick for vs while and avoid bugs. Many algorithms are loops with careful state updates.',
      'Practice counting, finding max/min, and early exit — foundations for later data structures and projects.',
    ],
    sandboxCode: 'nums = [4, 1, 9, 2]\nbest = nums[0]\nfor n in nums:\n    if n > best:\n        best = n\nprint("max", best)',
    challenges: [
      chHelp('loop_patterns', 0, 'easy', 'Count items', 'Count elements in [1,2,3].', pyVar('c', 'c == 3'), { starterCode: 'c=0\nfor _ in [1,2,3]: c+=1' }),
      chHelp('loop_patterns', 1, 'easy', 'Find max', 'Max of [3,9,2] into m.', pyVar('m', 'm == 9'), { starterCode: 'm=float("-inf")\nfor x in [3,9,2]:\n if x>m: m=x' }),
      chHelp('loop_patterns', 2, 'medium', 'Filter positives', 'Filter [ -1, 2, -3, 4 ] → [2,4].', pyVar('p', 'p == [2,4]'), { starterCode: 'p=[]\nfor x in [-1,2,-3,4]:\n if x>0: p.append(x)' }),
      chHelp('loop_patterns', 3, 'medium', 'Map double', 'Double each in [1,2,3].', pyVar('d', 'd == [2,4,6]'), { starterCode: 'd=[]\nfor x in [1,2,3]: d.append(x*2)' }),
      chHelp('loop_patterns', 4, 'hard', 'First index', 'Index of 5 in [2,5,7] → 1.', pyVar('i', 'i == 1'), { starterCode: 'i=-1\nfor j,v in enumerate([2,5,7]):\n if v==5: i=j; break' }),
      chHelp('loop_patterns', 5, 'hard', 'Count matches', 'Count 2 in [1,2,2,3].', pyVar('c', 'c == 2'), { starterCode: 'c=0\nfor x in [1,2,2,3]:\n if x==2: c+=1' }),
      chHelp('loop_patterns', 6, 'expert', 'Running average', 'Avg of [2,4,6] → 4.', pyVar('avg', 'avg == 4'), { starterCode: 'nums=[2,4,6]\ns=0\nfor n in nums: s+=n\navg=s/len(nums)' }),
      chHelp('loop_patterns', 7, 'expert', 'Fizz single', 'Fizz for 15: "FizzBuzz" check div 3 and 5.', pyVar('out', "out == 'FizzBuzz'"), { starterCode: 'n=15\nout=""\nif n%3==0: out+="Fizz"\nif n%5==0: out+="Buzz"' }),
      chHelp('loop_patterns', 8, 'expert', 'Reverse list', 'Reverse [1,2,3] without reversed().', pyVar('r', 'r == [3,2,1]'), { starterCode: 'r=[]\nfor x in [1,2,3]:\n r=[x]+r' }),
      chHelp('loop_patterns', 9, 'expert', 'Pair max', 'Max pair sum from [1,4,2] is 6.', pyVar('best', 'best == 6'), { starterCode: 'best=0\na=[1,4,2]\nfor i in range(len(a)):\n for j in range(i+1,len(a)):\n  best=max(best,a[i]+a[j])' }),
    ],
    questions: [
      qMcq('Accumulate pattern:', [{ id: 'a', label: 'Build sum in loop' }, { id: 'b', label: 'Import json' }, { id: 'c', label: 'Delete files' }], 'a'),
      qShort('Search pattern exits early with:', 'break'),
      qMcq('Filter builds:', [{ id: 'a', label: 'New list of matches' }, { id: 'b', label: 'Only prints' }, { id: 'c', label: 'GUI' }], 'a'),
      qShort('Map transforms each:', 'element'),
      qMcq('Finding max initializes with:', [{ id: 'a', label: 'First item or -inf' }, { id: 'b', label: 'Zero always' }, { id: 'c', label: 'None' }], 'a'),
      qMcq('enumerate helps:', [{ id: 'a', label: 'Index + value' }, { id: 'b', label: 'Sort only' }, { id: 'c', label: 'Compile' }], 'a'),
      qShort('FizzBuzz checks divisibility by:', '3 and 5'),
      qMcq('Expert: O(n) single pass is:', [{ id: 'a', label: 'Linear time' }, { id: 'b', label: 'Exponential' }, { id: 'c', label: 'Constant' }], 'a'),
      qMcq('Expert: nested pair loop is:', [{ id: 'a', label: 'O(n²) roughly' }, { id: 'b', label: 'O(1)' }, { id: 'c', label: 'O(log n)' }], 'a'),
      qShort('Expert: algorithm is steps to solve:', 'problem'),
    ],
  }),
]
