import { snippet } from './_builders.mjs'

/** @type {Record<string, { id: string, label: string, code: string }[]>} */
export const SANDBOX_SNIPPETS_BY_LEVEL = {
  // ── Control Flow ──────────────────────────────────────────────
  if_elif_else: [
    snippet('Grade A', 'score = 95\nif score >= 90:\n    print("A")'),
    snippet('elif chain', 'score = 85\nif score >= 90:\n    print("A")\nelif score >= 80:\n    print("B")\nelse:\n    print("C")'),
    snippet('else branch', 'n = 0\nif n:\n    print("yes")\nelse:\n    print("no")'),
    snippet('Nested if', 'a, b = 1, 2\nif a < b:\n    print("ok")'),
    snippet('Pass / Fail', 'score = 74\nif score >= 70:\n    print("Pass")\nelse:\n    print("Fail")'),
  ],
  boolean_logic: [
    snippet('and / or', 'print(True and False, True or False)'),
    snippet('not', 'print(not True, not False)'),
    snippet('in operator', 'print(3 in [1, 2, 3])'),
    snippet('any / all', 'print(any([False, True]), all([True, True]))'),
    snippet('is None', 'x = None\nprint(x is None)'),
  ],
  comparison_operators: [
    snippet('Equality', 'print(3 == 3, 3 != 4)'),
    snippet('Less than', 'print(2 < 5, 5 <= 5)'),
    snippet('String compare', 'print("a" < "b")'),
    snippet('Chained', 'print(1 < 2 < 3)'),
    snippet('is vs ==', 'a = [1]\nb = [1]\nprint(a == b, a is b)'),
  ],
  while_loops: [
    snippet('Countdown', 'n = 3\nwhile n > 0:\n    print(n)\n    n -= 1'),
    snippet('Sum loop', 'total = 0\nn = 1\nwhile n <= 5:\n    total += n\n    n += 1\nprint(total)'),
    snippet('Input pattern', 'count = 0\nwhile count < 3:\n    count += 1\nprint(count)'),
    snippet('break', 'n = 0\nwhile True:\n    n += 1\n    if n == 3:\n        break\nprint(n)'),
  ],
  for_loops: [
    snippet('Loop string', 'for ch in "hi":\n    print(ch)'),
    snippet('Loop list', 'for n in [10, 20, 30]:\n    print(n * 2)'),
    snippet('enumerate', 'for i, val in enumerate(["a", "b"]):\n    print(i, val)'),
    snippet('zip pairs', 'for a, b in zip([1, 2], ["x", "y"]):\n    print(a, b)'),
    snippet('for / else', 'for n in range(3):\n    pass\nelse:\n    print("done")'),
  ],
  range_function: [
    snippet('range(5)', 'print(list(range(5)))'),
    snippet('Start & stop', 'print(list(range(2, 8)))'),
    snippet('With step', 'print(list(range(2, 8, 2)))'),
    snippet('Squares', 'for i in range(4):\n    print(i ** 2)'),
    snippet('Downward', 'print(list(range(5, 0, -1)))'),
  ],
  break_continue: [
    snippet('break early', 'for n in range(10):\n    if n == 5:\n        break\n    print(n)'),
    snippet('continue skip', 'for n in range(5):\n    if n % 2 == 0:\n        continue\n    print(n)'),
    snippet('while break', 'while True:\n    print("once")\n    break'),
    snippet('Search loop', 'nums = [2, 4, 7, 8]\nfor n in nums:\n    if n == 7:\n        print("found")\n        break'),
  ],
  nested_loops: [
    snippet('Grid rows', 'for row in range(3):\n    print(list(range(row + 1)))'),
    snippet('Multiplication table', 'for i in range(1, 4):\n    for j in range(1, 4):\n        print(i * j, end=" ")\n    print()'),
    snippet('2D list', 'matrix = [[1, 2], [3, 4]]\nprint(matrix[1][0])'),
    snippet('Pair counter', 'count = 0\nfor i in range(3):\n    for j in range(3):\n        count += 1\nprint(count)'),
  ],
  match_case: [
    snippet('HTTP status', 'def status(n):\n    match n:\n        case 200: return "OK"\n        case 404: return "Not Found"\n        case _: return "Unknown"\nprint(status(404))'),
    snippet('case value', 'def label(x):\n    match x:\n        case 1: return "one"\n        case 2: return "two"\n        case _: return "other"\nprint(label(2))'),
    snippet('Default _', 'def kind(x):\n    match x:\n        case "a": return "letter"\n        case _: return "other"\nprint(kind("z"))'),
    snippet('Guard', 'def grade(n):\n    match n:\n        case x if x >= 90: return "A"\n        case _: return "B"\nprint(grade(95))'),
  ],
  loop_patterns: [
    snippet('Find max', 'nums = [4, 1, 9, 2]\nbest = nums[0]\nfor n in nums:\n    if n > best:\n        best = n\nprint(best)'),
    snippet('Accumulate sum', 'total = 0\nfor n in [1, 2, 3, 4]:\n    total += n\nprint(total)'),
    snippet('Filter evens', 'evens = [n for n in range(10) if n % 2 == 0]\nprint(evens)'),
    snippet('Count matches', 'count = 0\nfor w in ["cat", "car", "dog"]:\n    if w.startswith("c"):\n        count += 1\nprint(count)'),
    snippet('enumerate index', 'for i, word in enumerate(["a", "b", "c"]):\n    print(i, word)'),
  ],

  // ── Data Structures ───────────────────────────────────────────
  lists: [
    snippet('Create list', 'nums = [1, 2, 3]\nprint(nums)'),
    snippet('append', 'lst = [1, 2]\nlst.append(3)\nprint(lst)'),
    snippet('Index', 'lst = [10, 20, 30]\nprint(lst[0], lst[-1])'),
    snippet('Slice', 'lst = [0, 1, 2, 3]\nprint(lst[1:3])'),
    snippet('List comp', 'squares = [n * n for n in range(4)]\nprint(squares)'),
  ],
  tuples: [
    snippet('Tuple literal', 'point = (3, 4)\nprint(point)'),
    snippet('Unpack', 'x, y = point\nprint(x + y)'),
    snippet('Single item', 't = (1,)\nprint(t)'),
    snippet('Swap', 'a, b = 1, 2\na, b = b, a\nprint(a, b)'),
    snippet('As dict key', 'd = {(1, 2): "origin"}\nprint(d[(1, 2)])'),
  ],
  dictionaries: [
    snippet('Dict literal', 'user = {"name": "Ada", "score": 99}\nprint(user)'),
    snippet('get default', 'print(user.get("role", "guest"))'),
    snippet('Update key', 'user["score"] = 100\nprint(user)'),
    snippet('items loop', 'for k, v in user.items():\n    print(k, v)'),
    snippet('Dict comp', 'lengths = {w: len(w) for w in ["hi", "hey"]}\nprint(lengths)'),
  ],
  sets: [
    snippet('Set literal', 's = {1, 2, 2, 3}\nprint(s)'),
    snippet('Union', 'print({1, 2} | {2, 3})'),
    snippet('Intersection', 'print({1, 2, 3} & {2, 3, 4})'),
    snippet('Membership', 'print(2 in {1, 2, 3})'),
    snippet('Dedupe', 'unique = set([1, 1, 2, 2])\nprint(unique)'),
  ],
  list_comprehensions: [
    snippet('Basic', 'print([x for x in range(3)])'),
    snippet('Map', 'print([x * 2 for x in [1, 2, 3]])'),
    snippet('Filter evens', 'print([x for x in range(6) if x % 2 == 0])'),
    snippet('Squares', 'print([n * n for n in range(5)])'),
    snippet('Nested', 'pairs = [(i, j) for i in range(2) for j in range(2)]\nprint(pairs)'),
  ],
  dict_set_comprehensions: [
    snippet('Dict comp', 'print({x: x * x for x in range(3)})'),
    snippet('Set comp', 'print({c.lower() for c in "AaBb"})'),
    snippet('Len map', 'print({name: len(name) for name in ["Ada", "Bob"]})'),
    snippet('Filter set', 'print({x for x in range(5) if x % 2})'),
    snippet('Invert dict', 'd = {"a": 1, "b": 2}\nprint({v: k for k, v in d.items()})'),
  ],
  slicing_indexing: [
    snippet('First & last', 's = "Python"\nprint(s[0], s[-1])'),
    snippet('Slice', 'print(s[1:4])'),
    snippet('Step', 'print(s[::2])'),
    snippet('Reverse', 'print(s[::-1])'),
    snippet('List slice', 'print([1, 2, 3, 4][:2])'),
  ],
  iterating_collections: [
    snippet('for items', 'for x in [1, 2, 3]:\n    print(x)'),
    snippet('dict keys', 'for k in {"a": 1, "b": 2}:\n    print(k)'),
    snippet('items()', 'for k, v in {"a": 1}.items():\n    print(k, v)'),
    snippet('enumerate', 'for i, x in enumerate(["a", "b"]):\n    print(i, x)'),
    snippet('zip', 'for a, b in zip([1, 2], ["x", "y"]):\n    print(a, b)'),
  ],
  nested_structures: [
    snippet('2D index', 'matrix = [[1, 2], [3, 4]]\nprint(matrix[1][0])'),
    snippet('List of dicts', 'users = [{"name": "Ada", "score": 10}]\nprint(users[0]["name"])'),
    snippet('Nested dict', 'd = {"user": {"name": "Ana"}}\nprint(d["user"]["name"])'),
    snippet('Sum matrix', 'total = sum(v for row in [[1, 2], [3, 4]] for v in row)\nprint(total)'),
  ],
  choosing_structures: [
    snippet('List', 'queue = [1, 2]\nprint(type(queue).__name__)'),
    snippet('Dict lookup', 'phone = {"Ana": "555"}\nprint("Ana" in phone)'),
    snippet('Set unique', 'tags = set(["a", "a", "b"])\nprint(tags)'),
    snippet('Tuple point', 'point = (1, 2)\nprint(point)'),
    snippet('Count freq', 'counts = {}\nfor ch in "aba":\n    counts[ch] = counts.get(ch, 0) + 1\nprint(counts)'),
  ],

  // ── Functions ─────────────────────────────────────────────────
  defining_functions: [
    snippet('Simple def', 'def greet(name):\n    return f"Hello, {name}!"\nprint(greet("Ada"))'),
    snippet('No args', 'def hi():\n    return "hi"\nprint(hi())'),
    snippet('Two params', 'def add(a, b):\n    return a + b\nprint(add(2, 3))'),
    snippet('Early return', 'def abs_val(x):\n    if x < 0:\n        return -x\n    return x\nprint(abs_val(-5))'),
    snippet('No return', 'def f():\n    pass\nprint(f())'),
  ],
  parameters_arguments: [
    snippet('Positional', 'def power(base, exp):\n    return base ** exp\nprint(power(2, 10))'),
    snippet('Order matters', 'def sub(a, b):\n    return a - b\nprint(sub(5, 2))'),
    snippet('Three args', 'def sum3(a, b, c):\n    return a + b + c\nprint(sum3(1, 2, 3))'),
    snippet('Keyword call', 'def connect(host, port):\n    return f"{host}:{port}"\nprint(connect(port=443, host="localhost"))'),
    snippet('Nested call', 'def double(x):\n    return x * 2\nprint(double(double(2)))'),
  ],
  return_values: [
    snippet('Return int', 'def square(n):\n    return n * n\nprint(square(4))'),
    snippet('Multiple values', 'def min_max(nums):\n    return min(nums), max(nums)\nlo, hi = min_max([3, 1, 4])\nprint(lo, hi)'),
    snippet('Return list', 'def evens(n):\n    return [x for x in range(n) if x % 2 == 0]\nprint(evens(6))'),
    snippet('Guard return', 'def safe_div(a, b):\n    if b == 0:\n        return None\n    return a / b\nprint(safe_div(10, 2))'),
  ],
  default_parameters: [
    snippet('Default arg', 'def greet(name, punct="!"):\n    return f"Hi {name}{punct}"\nprint(greet("Ada"), greet("Bob", "?"))'),
    snippet('Optional flag', 'def connect(ssl=True):\n    return f"ssl={ssl}"\nprint(connect(), connect(ssl=False))'),
    snippet('None sentinel', 'def append_item(lst=None):\n    if lst is None:\n        lst = []\n    lst.append(1)\n    return lst\nprint(append_item())'),
  ],
  keyword_arguments: [
    snippet('Named args', 'def connect(host, port, ssl=True):\n    return f"{host}:{port} ssl={ssl}"\nprint(connect("localhost", port=443))'),
    snippet('Skip default', 'def f(a, b=10):\n    return a + b\nprint(f(5))'),
    snippet('Clear call', 'def create_user(name, role="guest", active=True):\n    return {"name": name, "role": role, "active": active}\nprint(create_user("Ana", role="admin"))'),
  ],
  args_kwargs: [
    snippet('*args', 'def total(*nums):\n    return sum(nums)\nprint(total(1, 2, 3))'),
    snippet('**kwargs', 'def show(**info):\n    return info\nprint(show(name="Ada", score=90))'),
    snippet('Unpack call', 'def add(a, b):\n    return a + b\nprint(add(*(2, 3)))'),
    snippet('Combined', 'def flex(a, *args, kwonly=0, **kwargs):\n    return (a, args, kwonly, kwargs)\nprint(flex(1, 2, kwonly=0, extra=True))'),
  ],
  scope_globals: [
    snippet('global', 'count = 0\n\ndef inc():\n    global count\n    count += 1\n\ninc()\nprint(count)'),
    snippet('Local shadow', 'x = 1\ndef f():\n    x = 2\n    return x\nprint(f(), x)'),
    snippet('Read global', 'name = "Ada"\ndef greet():\n    return f"Hi {name}"\nprint(greet())'),
    snippet('LEGB', 'def outer():\n    msg = "hi"\n    def inner():\n        return msg\n    return inner()\nprint(outer())'),
  ],
  lambda_functions: [
    snippet('lambda sort', 'nums = [3, 1, 4]\nprint(sorted(nums, key=lambda x: -x))'),
    snippet('lambda map', 'print(list(map(lambda x: x * 2, [1, 2, 3])))'),
    snippet('lambda filter', 'print(list(filter(lambda x: x % 2 == 0, [1, 2, 3, 4])))'),
    snippet('lambda max key', 'words = ["hi", "hello", "hey"]\nprint(max(words, key=lambda w: len(w)))'),
  ],
  recursion: [
    snippet('Factorial', 'def fact(n):\n    return 1 if n <= 1 else n * fact(n - 1)\nprint(fact(5))'),
    snippet('Countdown', 'def down(n):\n    if n == 0:\n        return\n    print(n)\n    down(n - 1)\ndown(3)'),
    snippet('Sum list', 'def sum_list(nums):\n    if not nums:\n        return 0\n    return nums[0] + sum_list(nums[1:])\nprint(sum_list([1, 2, 3]))'),
    snippet('Fibonacci', 'def fib(n):\n    if n <= 1:\n        return n\n    return fib(n - 1) + fib(n - 2)\nprint(fib(6))'),
  ],
  docstrings_hints: [
    snippet('Docstring', 'def area(radius: float) -> float:\n    """Return circle area."""\n    return 3.14159 * radius * radius\nprint(area.__doc__)'),
    snippet('Type hints', 'def greet(name: str) -> str:\n    return f"Hello, {name}"\nprint(greet("Ada"))'),
    snippet('Annotated def', 'def add(a: int, b: int) -> int:\n    return a + b\nprint(add(2, 3))'),
    snippet('help()', 'def double(x):\n    """Double a number."""\n    return x * 2\nprint(double.__doc__)'),
  ],

  // ── Modules ─────────────────────────────────────────────────────
  import_statements: [
    snippet('import math', 'import math\nprint(math.sqrt(16))'),
    snippet('from import', 'from math import pi\nprint(round(pi, 2))'),
    snippet('import as', 'import math as m\nprint(m.sqrt(25))'),
    snippet('random', 'import random\nprint(random.randint(1, 6))'),
    snippet('json', 'import json\nprint(json.loads(\'{"a": 1}\'))'),
  ],
  stdlib_overview: [
    snippet('os.getcwd', 'import os\nprint(os.getcwd())'),
    snippet('sys version', 'import sys\nprint(sys.version_info.major)'),
    snippet('pathlib', 'from pathlib import Path\nprint(Path("data.txt").suffix)'),
    snippet('collections', 'from collections import Counter\nprint(Counter("aab"))'),
    snippet('datetime', 'import datetime\nprint(datetime.date.today())'),
  ],
  math_module: [
    snippet('sqrt', 'import math\nprint(math.sqrt(16))'),
    snippet('pi', 'import math\nprint(round(math.pi, 2))'),
    snippet('floor / ceil', 'import math\nprint(math.floor(3.9), math.ceil(3.1))'),
    snippet('degrees', 'import math\nprint(math.degrees(math.pi))'),
    snippet('pow', 'import math\nprint(math.pow(2, 8))'),
  ],
  random_module: [
    snippet('randint', 'import random\nprint(random.randint(1, 10))'),
    snippet('choice', 'import random\nprint(random.choice(["red", "blue", "green"]))'),
    snippet('shuffle', 'import random\nlst = [1, 2, 3]\nrandom.shuffle(lst)\nprint(lst)'),
    snippet('seed', 'import random\nrandom.seed(0)\nprint(random.randint(1, 100))'),
    snippet('random()', 'import random\nprint(round(random.random(), 2))'),
  ],
  datetime_module: [
    snippet('today', 'from datetime import date\ntoday = date.today()\nprint(today)'),
    snippet('timedelta', 'from datetime import date, timedelta\nprint(date.today() + timedelta(days=7))'),
    snippet('strftime', 'from datetime import datetime\nprint(datetime.now().strftime("%Y-%m-%d"))'),
    snippet('datetime now', 'from datetime import datetime\nprint(datetime.now().year)'),
    snippet('Parse date', 'from datetime import datetime\nd = datetime.strptime("2024-06-01", "%Y-%m-%d")\nprint(d.month)'),
  ],
  creating_modules: [
    snippet('Function module', 'def add(a, b):\n    return a + b\nprint(add(1, 2))'),
    snippet('Constants', 'PI = 3.14159\nprint(PI)'),
    snippet('Private name', '_helper = "internal"\nprint(_helper)'),
    snippet('Module docstring', '"""My utility module."""\nprint(__doc__)'),
  ],
  packages: [
    snippet('Package import', '# from mypkg import utils\n# utils.add(1, 2)\nprint("package layout")'),
    snippet('__init__', '# mypkg/__init__.py exposes API\nprint("package init")'),
    snippet('Relative import', '# from . import sibling\nprint("relative import")'),
    snippet('Nested package', '# mypkg/subpkg/module.py\nprint("nested")'),
  ],
  pip_venv: [
    snippet('requirements', '# pip freeze > requirements.txt\nprint("requirements.txt")'),
    snippet('venv note', '# python -m venv .venv\nprint("virtual environment")'),
    snippet('pip install', '# pip install requests\nprint("pip install")'),
    snippet('import third party', '# import requests\nprint("third-party package")'),
  ],
  name_main: [
    snippet('main guard', 'def main():\n    print("Running main")\n\nif __name__ == "__main__":\n    main()'),
    snippet('__name__', 'print(__name__)'),
    snippet('Importable fn', 'def helper():\n    return 42\n\nif __name__ == "__main__":\n    print(helper())'),
    snippet('Demo block', 'def demo():\n    print("demo")\n\nif __name__ == "__main__":\n    demo()'),
  ],
  organizing_projects: [
    snippet('Project layout', '# myapp/\n#   main.py\n#   utils.py\n# tests/\nprint("project structure")'),
    snippet('README', '# README.md explains install & usage\nprint("README")'),
    snippet('tests folder', '# tests/test_utils.py\nprint("tests/")'),
    snippet('src layout', '# src/myapp/__init__.py\nprint("src layout")'),
  ],

  // ── File Handling ─────────────────────────────────────────────
  opening_files: [
    snippet('with open', '# with open("notes.txt") as f:\n#     data = f.read()\nprint("with open pattern")'),
    snippet('pathlib Path', 'from pathlib import Path\np = Path("data.txt")\nprint(p.name, p.suffix)'),
    snippet('Read mode', 'mode = "r"\nprint(mode)'),
    snippet('Write mode', 'mode = "w"\nprint(mode)'),
    snippet('StringIO demo', 'from io import StringIO\nf = StringIO("hello")\nprint(f.read())'),
  ],
  reading_files: [
    snippet('splitlines', 'text = "line1\\nline2"\nprint(text.splitlines())'),
    snippet('strip line', 'print("hi\\n".strip())'),
    snippet('for line', 'for line in "a\\nb\\nc".splitlines():\n    print(line)'),
    snippet('readline', 'from io import StringIO\nline = StringIO("a\\nb").readline().strip()\nprint(line)'),
    snippet('Count lines', 'print(len("a\\nb\\nc".splitlines()))'),
  ],
  writing_files: [
    snippet('write text', 'from io import StringIO\nbuf = StringIO()\nbuf.write("hello\\n")\nprint(buf.getvalue())'),
    snippet('writelines', 'lines = ["one\\n", "two\\n"]\nprint("".join(lines))'),
    snippet('Append newline', 'line = "event"\nprint(line + "\\n")'),
    snippet('StringIO write', 'from io import StringIO\nbuf = StringIO()\nbuf.write("data")\nprint(buf.getvalue())'),
  ],
  appending: [
    snippet('Append mode', '# with open("log.txt", "a") as f:\n#     f.write("event\\n")\nprint("append mode")'),
    snippet('Build log', 'log = []\nlog.append("start")\nlog.append("end")\nprint(log)'),
    snippet('Join log lines', 'lines = ["2024-01-01 ok", "2024-01-02 ok"]\nprint("\\n".join(lines))'),
  ],
  with_statement: [
    snippet('with demo', 'class Demo:\n    def __enter__(self):\n        print("enter")\n        return self\n    def __exit__(self, *args):\n        print("exit")\n\nwith Demo():\n    print("body")'),
    snippet('StringIO with', 'from io import StringIO\nwith StringIO("hi") as f:\n    print(f.read())'),
    snippet('Auto close', '# with open("f.txt") as f:\n#     pass  # closes automatically\nprint("auto close")'),
  ],
  file_paths: [
    snippet('Path join', 'from pathlib import Path\nbase = Path("project")\nfile = base / "data" / "scores.csv"\nprint(file)'),
    snippet('name parts', 'from pathlib import Path\np = Path("data/scores.csv")\nprint(p.parent, p.name, p.suffix)'),
    snippet('exists check', 'from pathlib import Path\nprint(Path(".").exists())'),
    snippet('stem', 'from pathlib import Path\nprint(Path("report.csv").stem)'),
  ],
  csv_files: [
    snippet('DictReader', 'import csv\nfrom io import StringIO\nbuf = StringIO("name,score\\nAda,99\\n")\nrows = list(csv.DictReader(buf))\nprint(rows[0])'),
    snippet('csv writer', 'import csv\nfrom io import StringIO\nbuf = StringIO()\nw = csv.writer(buf)\nw.writerow(["a", "b"])\nprint(buf.getvalue())'),
    snippet('Read rows', 'import csv\nfrom io import StringIO\ndata = "a,b\\n1,2\\n"\nprint(list(csv.reader(StringIO(data))))'),
    snippet('Delimiter', 'import csv\nfrom io import StringIO\nbuf = StringIO()\ncsv.writer(buf, delimiter=";").writerow(["a", "b"])\nprint(buf.getvalue())'),
  ],
  json_files: [
    snippet('dumps', 'import json\ndata = {"name": "Ada", "scores": [10, 20]}\nprint(json.dumps(data, indent=2))'),
    snippet('loads', 'import json\nback = json.loads(\'{"name": "Ada"}\')\nprint(back["name"])'),
    snippet('Round trip', 'import json\ndata = {"a": 1}\nprint(json.loads(json.dumps(data)))'),
    snippet('List JSON', 'import json\nprint(json.loads("[1, 2, 3]"))'),
  ],
  text_vs_binary: [
    snippet('encode', 'text = "hi"\nb = text.encode("utf-8")\nprint(b)'),
    snippet('decode', 'b = b"hello"\nprint(b.decode("utf-8"))'),
    snippet('bytes type', 'print(type(b"hello").__name__)'),
    snippet('str type', 'print(type("hello").__name__)'),
  ],
  file_errors: [
    snippet('FileNotFound', 'try:\n    open("missing.txt")\nexcept FileNotFoundError:\n    print("File missing")'),
    snippet('Permission', 'try:\n    raise PermissionError("denied")\nexcept PermissionError as e:\n    print(e)'),
    snippet('Safe message', 'path = "data.txt"\ntry:\n    open(path)\nexcept FileNotFoundError:\n    print(f"Could not open {path}")'),
  ],

  // ── Error Handling ────────────────────────────────────────────
  try_except: [
    snippet('ValueError', 'try:\n    int("abc")\nexcept ValueError as e:\n    print("Bad number:", e)'),
    snippet('Recover', 'try:\n    n = int("bad")\nexcept ValueError:\n    n = 0\nprint(n)'),
    snippet('ZeroDivision', 'try:\n    1 / 0\nexcept ZeroDivisionError:\n    print("Cannot divide by zero")'),
    snippet('as e', 'try:\n    int("x")\nexcept ValueError as e:\n    print(str(e))'),
    snippet('EAFP KeyError', 'd = {}\ntry:\n    val = d["missing"]\nexcept KeyError:\n    val = None\nprint(val)'),
  ],
  multiple_except: [
    snippet('KeyError', 'try:\n    print({}["x"])\nexcept KeyError:\n    print("missing key")'),
    snippet('Two handlers', 'try:\n    int("x")\nexcept ValueError:\n    print("value")\nexcept TypeError:\n    print("type")'),
    snippet('Tuple except', 'try:\n    int("a")\nexcept (ValueError, TypeError):\n    print("caught")'),
    snippet('Specific first', 'try:\n    int("x")\nexcept ValueError:\n    print("specific")\nexcept Exception:\n    print("broad")'),
  ],
  else_finally: [
    snippet('try/else', 'try:\n    int("42")\nexcept ValueError:\n    print("fail")\nelse:\n    print("success")'),
    snippet('finally', 'try:\n    print("try")\nfinally:\n    print("finally")'),
    snippet('Cleanup', 'resource = "open"\ntry:\n    print(resource)\nfinally:\n    resource = "closed"\nprint(resource)'),
  ],
  raising_exceptions: [
    snippet('raise ValueError', 'def pct(n):\n    if not 0 <= n <= 100:\n        raise ValueError("n must be 0-100")\n    return f"{n}%"\nprint(pct(50))'),
    snippet('raise TypeError', 'def need_int(x):\n    if not isinstance(x, int):\n        raise TypeError("int required")\n    return x\nprint(need_int(3))'),
    snippet('Fail fast', 'def divide(a, b):\n    if b == 0:\n        raise ValueError("b cannot be 0")\n    return a / b\nprint(divide(10, 2))'),
  ],
  custom_exceptions: [
    snippet('Custom class', 'class ScoreError(Exception):\n    pass\n\ndef set_score(n):\n    if n < 0:\n        raise ScoreError("negative score")\n    return n\nprint(set_score(10))'),
    snippet('With message', 'class AppError(Exception):\n    pass\n\ntry:\n    raise AppError("something went wrong")\nexcept AppError as e:\n    print(e)'),
    snippet('Subclass', 'class ValidationError(Exception):\n    pass\n\nprint(issubclass(ValidationError, Exception))'),
  ],
  exception_hierarchy: [
    snippet('MRO', 'print(ValueError.__mro__)'),
    snippet('ZeroDivision', 'try:\n    1 / 0\nexcept ZeroDivisionError:\n    print("math error")'),
    snippet('Catch parent', 'try:\n    int("x")\nexcept Exception:\n    print("caught Exception")'),
    snippet('BaseException', 'print(issubclass(KeyboardInterrupt, BaseException))'),
  ],
  assertions: [
    snippet('assert basic', 'def split_even(total, parts):\n    assert parts > 0, "parts must be positive"\n    return total / parts\nprint(split_even(10, 2))'),
    snippet('assert type', 'value = 5\nassert isinstance(value, int)\nprint("ok")'),
    snippet('assert range', 'age = 30\nassert age >= 0\nprint(age)'),
  ],
  debugging: [
    snippet('breakpoint', '# breakpoint()  # debugger pause\nx = 2\nprint(x + 1)'),
    snippet('Print debug', 'total = 0\nfor n in [1, 2, 3]:\n    total += n\n    print("debug total:", total)'),
    snippet('repr inspect', 'data = {"a": 1}\nprint(repr(data))'),
    snippet('Minimal repro', 'def buggy(a):\n    return a + 1\nprint(buggy(2))'),
  ],
  logging_basics: [
    snippet('basicConfig', 'import logging\nlogging.basicConfig(level=logging.INFO)\nlog = logging.getLogger(__name__)\nlog.info("App started")'),
    snippet('warning', 'import logging\nlogging.basicConfig(level=logging.WARNING)\nlogging.warning("Low disk")'),
    snippet('error', 'import logging\nlogging.basicConfig(level=logging.ERROR)\nlogging.error("Failed to connect")'),
    snippet('levels', 'import logging\nprint(logging.INFO, logging.WARNING, logging.ERROR)'),
  ],
  defensive_programming: [
    snippet('Validate input', 'def average(nums):\n    if not nums:\n        raise ValueError("nums cannot be empty")\n    return sum(nums) / len(nums)\nprint(average([2, 4]))'),
    snippet('Default fallback', 'def greet(name=None):\n    name = name or "guest"\n    return f"Hi {name}"\nprint(greet())'),
    snippet('Bounds check', 'def clamp(n, lo, hi):\n    return max(lo, min(hi, n))\nprint(clamp(15, 0, 10))'),
    snippet('Guard clause', 'def divide(a, b):\n    if b == 0:\n        raise ValueError("b cannot be 0")\n    return a / b\nprint(divide(10, 2))'),
    snippet('Type check', 'def need_str(x):\n    if not isinstance(x, str):\n        raise TypeError("str required")\n    return x\nprint(need_str("ok"))'),
  ],

  // ── Object Oriented ───────────────────────────────────────────
  classes_basics: [
    snippet('Define class', 'class Cat:\n    pass\n\nc = Cat()\nprint(c)'),
    snippet('__init__', 'class Dog:\n    def __init__(self, name):\n        self.name = name\n\nd = Dog("Rex")\nprint(d.name)'),
    snippet('Method', 'class Dog:\n    def bark(self):\n        return "woof"\nprint(Dog().bark())'),
    snippet('Two instances', 'class Point:\n    def __init__(self, x):\n        self.x = x\nprint(Point(1).x, Point(2).x)'),
    snippet('isinstance', 'class Dog:\n    pass\nprint(isinstance(Dog(), Dog))'),
  ],
  methods: [
    snippet('inc counter', 'class Counter:\n    def __init__(self):\n        self.n = 0\n    def inc(self):\n        self.n += 1\n    def value(self):\n        return self.n\nc = Counter()\nc.inc()\nprint(c.value())'),
    snippet('Return self', 'class Builder:\n    def add(self, x):\n        self.x = x\n        return self\nb = Builder().add(1)\nprint(b.x)'),
    snippet('staticmethod', 'class Math:\n    @staticmethod\n    def add(a, b):\n        return a + b\nprint(Math.add(2, 3))'),
    snippet('classmethod', 'class User:\n    def __init__(self, name):\n        self.name = name\n    @classmethod\n    def guest(cls):\n        return cls("guest")\nprint(User.guest().name)'),
  ],
  inheritance: [
    snippet('Override', 'class Animal:\n    def speak(self):\n        return "..."\n\nclass Dog(Animal):\n    def speak(self):\n        return "woof"\nprint(Dog().speak())'),
    snippet('super()', 'class Animal:\n    def __init__(self, name):\n        self.name = name\n\nclass Dog(Animal):\n    def __init__(self, name):\n        super().__init__(name)\n\nd = Dog("Rex")\nprint(d.name)'),
    snippet('Subclass check', 'class Animal:\n    pass\nclass Dog(Animal):\n    pass\nprint(issubclass(Dog, Animal))'),
  ],
  encapsulation: [
    snippet('Private attr', 'class BankAccount:\n    def __init__(self, balance=0):\n        self._balance = balance\n    def deposit(self, amount):\n        self._balance += amount\n    def get_balance(self):\n        return self._balance\na = BankAccount(100)\na.deposit(50)\nprint(a.get_balance())'),
    snippet('property getter', 'class Circle:\n    def __init__(self, r):\n        self._r = r\n    @property\n    def radius(self):\n        return self._r\nc = Circle(5)\nprint(c.radius)'),
    snippet('Validate setter', 'class Circle:\n    def __init__(self, r):\n        self.radius = r\n    @property\n    def radius(self):\n        return self._r\n    @radius.setter\n    def radius(self, value):\n        if value < 0:\n            raise ValueError("negative radius")\n        self._r = value\nc = Circle(3)\nprint(c.radius)'),
  ],
  polymorphism: [
    snippet('Duck typing', 'def announce(animal):\n    print(animal.speak())\n\nclass Cat:\n    def speak(self):\n        return "meow"\n\nclass Dog:\n    def speak(self):\n        return "woof"\n\nfor a in [Cat(), Dog()]:\n    print(a.speak())'),
    snippet('Shared interface', 'class Square:\n    def area(self):\n        return 4\n\nclass Circle:\n    def area(self):\n        return 3\n\nfor shape in [Square(), Circle()]:\n    print(shape.area())'),
  ],
  dunder_methods: [
    snippet('__repr__', 'class Point:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n    def __repr__(self):\n        return f"Point({self.x}, {self.y})"\nprint(repr(Point(1, 2)))'),
    snippet('__str__', 'class Greeter:\n    def __str__(self):\n        return "hello"\nprint(str(Greeter()))'),
    snippet('__eq__', 'class Point:\n    def __init__(self, x):\n        self.x = x\n    def __eq__(self, other):\n        return self.x == other.x\nprint(Point(1) == Point(1))'),
    snippet('__len__', 'class Bag:\n    def __init__(self, items):\n        self.items = items\n    def __len__(self):\n        return len(self.items)\nprint(len(Bag([1, 2, 3])))'),
  ],
  properties: [
    snippet('Read-only', 'class Circle:\n    def __init__(self, r):\n        self._r = r\n    @property\n    def radius(self):\n        return self._r\nc = Circle(5)\nprint(c.radius)'),
    snippet('Setter', 'class Circle:\n    def __init__(self, r):\n        self._r = r\n    @property\n    def radius(self):\n        return self._r\n    @radius.setter\n    def radius(self, value):\n        self._r = value\nc = Circle(5)\nc.radius = 10\nprint(c.radius)'),
    snippet('Computed', 'class Rectangle:\n    def __init__(self, w, h):\n        self.w, self.h = w, h\n    @property\n    def area(self):\n        return self.w * self.h\nprint(Rectangle(4, 5).area)'),
  ],
  dataclasses: [
    snippet('@dataclass', 'from dataclasses import dataclass\n\n@dataclass\nclass User:\n    name: str\n    score: int = 0\n\nprint(User("Ana", 42))'),
    snippet('Default field', 'from dataclasses import dataclass, field\n\n@dataclass\nclass Team:\n    name: str\n    members: list = field(default_factory=list)\n\nt = Team("A")\nprint(t)'),
    snippet('Compare', 'from dataclasses import dataclass\n\n@dataclass\nclass Point:\n    x: int\n    y: int\n\nprint(Point(1, 2) == Point(1, 2))'),
  ],
  special_methods_adv: [
    snippet('Composition', 'class Engine:\n    def start(self):\n        return "vroom"\n\nclass Car:\n    def __init__(self, engine):\n        self.engine = engine\n    def drive(self):\n        return self.engine.start()\nprint(Car(Engine()).drive())'),
    snippet('Inject dependency', 'class Logger:\n    def log(self, msg):\n        return msg\n\nclass App:\n    def __init__(self, logger):\n        self.logger = logger\n    def run(self):\n        return self.logger.log("ok")\nprint(App(Logger()).run())'),
    snippet('Factory', 'def make_user(name):\n    return {"name": name, "active": True}\nprint(make_user("Ana"))'),
  ],
  oop_design: [
    snippet('Abstract style', 'class Notifier:\n    def send(self, message: str) -> None:\n        raise NotImplementedError\n\nclass EmailNotifier(Notifier):\n    def send(self, message: str) -> None:\n        print(f"Email: {message}")\nEmailNotifier().send("Hello")'),
    snippet('Single responsibility', 'class Report:\n    def build(self, data):\n        return str(data)\n\nclass Printer:\n    def print(self, text):\n        print(text)'),
    snippet('Interface', 'class Shape:\n    def area(self):\n        raise NotImplementedError\n\nclass Square(Shape):\n    def area(self):\n        return 1\nprint(Square().area())'),
  ],

  // ── Data and APIs ─────────────────────────────────────────────
  json_basics: [
    snippet('dumps', 'import json\ndata = {"name": "Ana", "score": 95}\nprint(json.dumps(data))'),
    snippet('loads', 'import json\nprint(json.loads(\'{"name": "Ana"}\')["name"])'),
    snippet('Pretty print', 'import json\nprint(json.dumps({"a": 1}, indent=2))'),
    snippet('List JSON', 'import json\nprint(json.loads("[1, 2]"))'),
    snippet('Decode error', 'import json\ntry:\n    json.loads("{bad}")\nexcept json.JSONDecodeError:\n    print("invalid JSON")'),
  ],
  csv_data: [
    snippet('DictReader', 'import csv\nfrom io import StringIO\nbuf = StringIO("name,score\\nAna,90\\n")\nrows = list(csv.DictReader(buf))\nprint(rows[0]["name"])'),
    snippet('csv writer', 'import csv\nfrom io import StringIO\nbuf = StringIO()\ncsv.writer(buf).writerow(["a", "b"])\nprint(buf.getvalue())'),
    snippet('Sum column', 'import csv\nfrom io import StringIO\nbuf = StringIO("score\\n90\\n85\\n")\nprint(sum(int(r["score"]) for r in csv.DictReader(buf)))'),
    snippet('Delimiter', 'import csv\nfrom io import StringIO\nbuf = StringIO()\ncsv.writer(buf, delimiter=";").writerow(["a", "b"])\nprint(buf.getvalue())'),
  ],
  urllib_requests: [
    snippet('urlopen', 'from urllib.request import urlopen\nprint("urllib ready")'),
    snippet('Parse url', 'from urllib.parse import urlparse\nprint(urlparse("https://example.com/path"))'),
    snippet('quote', 'from urllib.parse import quote\nprint(quote("hello world"))'),
    snippet('Request headers', '# req = Request(url, headers={"User-Agent": "CodeQuest"})\nprint("custom headers")'),
  ],
  rest_apis: [
    snippet('Response dict', 'response = {"status": 200, "data": {"id": 1, "title": "Todo"}}\nprint(response["status"])'),
    snippet('Status codes', 'print(200, 404, 500)'),
    snippet('JSON body', 'import json\ndata = {"id": 1, "title": "Todo"}\nprint(json.dumps(data))'),
    snippet('GET POST', 'methods = ["GET", "POST", "PUT", "DELETE"]\nprint(methods)'),
  ],
  datetime_data: [
    snippet('now', 'from datetime import datetime\nprint(datetime.now().strftime("%Y-%m-%d"))'),
    snippet('timedelta', 'from datetime import datetime, timedelta\nprint((datetime.now() + timedelta(days=7)).date())'),
    snippet('Parse', 'from datetime import datetime\nd = datetime.strptime("2024-06-01", "%Y-%m-%d")\nprint(d.month)'),
    snippet('ISO date', 'from datetime import date\nprint(date.today().isoformat())'),
  ],
  collections_data: [
    snippet('Counter', 'from collections import Counter\nprint(Counter("aab"))'),
    snippet('most_common', 'from collections import Counter\nprint(Counter("aab").most_common(1))'),
    snippet('defaultdict', 'from collections import defaultdict\ndd = defaultdict(int)\nfor w in ["a", "b", "a"]:\n    dd[w] += 1\nprint(dict(dd))'),
    snippet('deque', 'from collections import deque\nd = deque([1, 2])\nd.appendleft(0)\nprint(list(d))'),
  ],
  pathlib_data: [
    snippet('Join path', 'from pathlib import Path\np = Path("data") / "scores.json"\nprint(p)'),
    snippet('suffix stem', 'from pathlib import Path\np = Path("scores.json")\nprint(p.suffix, p.stem)'),
    snippet('exists', 'from pathlib import Path\nprint(Path(".").exists())'),
    snippet('glob', 'from pathlib import Path\nprint(list(Path(".").glob("*.py"))[:3])'),
  ],
  sqlite_basics: [
    snippet('In-memory DB', 'import sqlite3\nconn = sqlite3.connect(":memory:")\nconn.execute("CREATE TABLE users (id INTEGER, name TEXT)")\nconn.execute("INSERT INTO users VALUES (1, ?)", ("Ana",))\nprint(conn.execute("SELECT name FROM users").fetchone()[0])'),
    snippet('Parameterized', 'import sqlite3\nconn = sqlite3.connect(":memory:")\nconn.execute("CREATE TABLE t (v INTEGER)")\nconn.execute("INSERT INTO t VALUES (?)", (42,))\nprint(conn.execute("SELECT v FROM t WHERE v=?", (42,)).fetchone()[0])'),
    snippet('fetchall', 'import sqlite3\nconn = sqlite3.connect(":memory:")\nconn.execute("CREATE TABLE t (v INTEGER)")\nconn.executemany("INSERT INTO t VALUES (?)", [(1,), (2,)])\nprint(conn.execute("SELECT v FROM t").fetchall())'),
  ],
  pandas_intro: [
    snippet('DataFrame', 'try:\n    import pandas as pd\n    df = pd.DataFrame({"name": ["Ana", "Bob"], "score": [90, 85]})\n    print(df)\nexcept ImportError:\n    print("pandas not installed")'),
    snippet('Mean column', 'try:\n    import pandas as pd\n    df = pd.DataFrame({"score": [90, 85]})\n    print(df["score"].mean())\nexcept ImportError:\n    print("pandas not installed")'),
    snippet('head()', 'try:\n    import pandas as pd\n    df = pd.DataFrame({"a": [1, 2, 3]})\n    print(df.head(2))\nexcept ImportError:\n    print("pandas not installed")'),
  ],
  data_validation: [
    snippet('Validate user', 'def validate_user(data):\n    if "name" not in data or not isinstance(data["name"], str):\n        raise ValueError("name required string")\n    if "age" in data and data["age"] < 0:\n        raise ValueError("age must be non-negative")\n    return data\nprint(validate_user({"name": "Ana", "age": 30}))'),
    snippet('Required fields', 'required = ["name", "email"]\ndata = {"name": "Ana", "email": "a@b.c"}\nprint(all(k in data for k in required))'),
    snippet('Type check', 'def is_int(x):\n    return isinstance(x, int)\nprint(is_int(5), is_int("5"))'),
    snippet('Range check', 'age = 30\nprint(0 <= age <= 120)'),
  ],

  // ── Projects section topics ───────────────────────────────────
  cli_guess_game: [
    snippet('randint', 'import random\nprint(random.randint(1, 10))'),
    snippet('while guess', 'secret = 7\nguess = 0\nwhile guess != secret:\n    guess = 7\nprint("win")'),
    snippet('Higher/Lower', 'g, s = 3, 5\nif g < s:\n    print("Higher")\nelif g > s:\n    print("Lower")'),
    snippet('main guard', 'def main():\n    print("play")\n\nif __name__ == "__main__":\n    main()'),
    snippet('try int input', 'try:\n    n = int("5")\nexcept ValueError:\n    n = 0\nprint(n)'),
  ],
  todo_list: [
    snippet('List of dicts', 'todos = [{"id": 1, "text": "Learn Python", "done": False}]\nprint(todos)'),
    snippet('add task', 'todos = []\ntodos.append({"id": 1, "text": "Build", "done": False})\nprint(todos)'),
    snippet('List tasks', 'todos = [{"text": "a", "done": False}, {"text": "b", "done": True}]\nfor t in todos:\n    mark = "x" if t["done"] else " "\n    print(f"[{mark}] {t[\'text\']}")'),
    snippet('json save', 'import json\ntodos = [{"text": "a"}]\nprint(json.dumps(todos))'),
    snippet('Filter done', 'todos = [{"done": True}, {"done": False}]\nopen_tasks = [t for t in todos if not t["done"]]\nprint(len(open_tasks))'),
  ],
  password_generator: [
    snippet('random.choice', 'import random\nimport string\nprint(random.choice(string.ascii_letters))'),
    snippet('Join password', 'import random\nimport string\nchars = string.ascii_letters + string.digits\nprint("".join(random.choice(chars) for _ in range(8)))'),
    snippet('generate fn', 'import random\nimport string\n\ndef generate_password(n=12):\n    chars = string.ascii_letters + string.digits\n    return "".join(random.choice(chars) for _ in range(n))\nprint(generate_password(8))'),
    snippet('secrets', 'import secrets\nprint(secrets.choice("abc"))'),
  ],
  unit_converter: [
    snippet('km to miles', 'KM_TO_MI = 0.621371\nprint(10 * KM_TO_MI)'),
    snippet('C to F', 'def c_to_f(c):\n    return c * 9/5 + 32\nprint(c_to_f(0))'),
    snippet('Conversion dict', 'factors = {"km": 1000, "m": 1}\nprint(factors["km"])'),
    snippet('Convert fn', 'def km_to_miles(km):\n    return km * 0.621371\nprint(km_to_miles(10))'),
  ],
  contact_book: [
    snippet('Dict storage', 'contacts = {}\ncontacts["ana"] = {"phone": "555"}\nprint(contacts)'),
    snippet('add contact', 'contacts = {}\ndef add(name, phone):\n    contacts[name.lower()] = {"phone": phone}\nadd("Ana", "555")\nprint(contacts)'),
    snippet('get lookup', 'contacts = {"ana": {"phone": "555"}}\nprint(contacts.get("bob"))'),
    snippet('Search partial', 'contacts = {"ana": 1, "andrew": 2}\nprint([k for k in contacts if "an" in k])'),
  ],
  quiz_app: [
    snippet('Question dict', 'questions = [{"q": "2+2?", "choices": ["3", "4"], "a": 1}]\nprint(questions[0]["choices"])'),
    snippet('Grade answers', 'questions = [{"a": 1}, {"a": 0}]\nanswers = [1, 0]\nscore = sum(1 for i, a in enumerate(answers) if a == questions[i]["a"])\nprint(score)'),
    snippet('Percent', 'print(int(3 / 4 * 100))'),
    snippet('shuffle', 'import random\nlst = [1, 2, 3]\nrandom.shuffle(lst)\nprint(lst)'),
  ],
  expense_tracker: [
    snippet('Add expense', 'expenses = []\nexpenses.append({"amount": 12.5, "category": "food"})\nprint(expenses)'),
    snippet('Sum amounts', 'expenses = [{"amount": 10}, {"amount": 5}]\nprint(sum(e["amount"] for e in expenses))'),
    snippet('Filter category', 'expenses = [{"category": "food", "amount": 1}, {"category": "bus", "amount": 2}]\nprint([e for e in expenses if e["category"] == "food"])'),
    snippet('strftime date', 'from datetime import datetime\nprint(datetime.now().strftime("%Y-%m-%d"))'),
    snippet('Group totals', 'totals = {}\nfor e in [{"category": "food", "amount": 10}, {"category": "food", "amount": 5}]:\n    totals[e["category"]] = totals.get(e["category"], 0) + e["amount"]\nprint(totals)'),
  ],
  web_scraper_basics: [
    snippet('Split parse', 'html = \'<a href="/page">Link</a>\'\nparts = html.split("<a href=")\nprint(len(parts))'),
    snippet('Extract href', 'part = \'"/page">Link\'\nurl = part.split(">")[0].strip(\'"\')\nprint(url)'),
    snippet('Build links', 'links = []\nlinks.append("/page")\nprint(links)'),
    snippet('urlopen', 'from urllib.request import urlopen\nprint("fetch ready")'),
    snippet('sleep rate limit', 'import time\ntime.sleep(0)\nprint("rate limit")'),
  ],
  weather_cli: [
    snippet('Mock API', 'API_SAMPLE = {"city": "London", "temp_c": 18, "conditions": "Cloudy"}\nprint(API_SAMPLE)'),
    snippet('Format report', 'data = {"city": "London", "temp_c": 18, "conditions": "Cloudy", "humidity": 72}\nprint(f"{data[\'city\']}: {data[\'temp_c\']}C, {data[\'conditions\']}")'),
    snippet('get nested', 'data = {}\nprint(data.get("wind", "Unknown"))'),
    snippet('json cache', 'import json\ndata = {"city": "London", "temp_c": 18}\nprint(json.dumps(data))'),
    snippet('argparse', 'import argparse\np = argparse.ArgumentParser()\np.add_argument("--city", default="Paris")\nprint(p.parse_args([]).city)'),
  ],
  capstone_planning: [
    snippet('Score fn', 'def project_score(features_done, tests_done):\n    return features_done * 10 + tests_done * 15\nprint(project_score(4, 2))'),
    snippet('main guard', 'def main():\n    print("Capstone ready")\n\nif __name__ == "__main__":\n    main()'),
    snippet('Module list', 'modules = ["models", "cli", "utils"]\nprint("cli" in modules)'),
    snippet('try/except plan', 'try:\n    int("42")\nexcept ValueError:\n    print("handle bad input")'),
    snippet('README sections', 'sections = ["Install", "Usage", "License"]\nprint(len(sections))'),
  ],

  // ── Section projects ────────────────────────────────────────────
  fizzbuzz_mini: [
    snippet('range 1-15', 'print(list(range(1, 16)))'),
    snippet('Modulo 3', 'print(9 % 3 == 0)'),
    snippet('Append Fizz', 'out = []\nn = 3\nif n % 3 == 0:\n    out.append("Fizz")\nprint(out)'),
    snippet('FizzBuzz branch', 'n = 15\nif n % 15 == 0:\n    print("FizzBuzz")'),
    snippet('Full loop', 'out = []\nfor n in range(1, 16):\n    if n % 15 == 0:\n        out.append("FizzBuzz")\n    elif n % 3 == 0:\n        out.append("Fizz")\n    elif n % 5 == 0:\n        out.append("Buzz")\n    else:\n        out.append(str(n))\nprint(out)'),
  ],
  grade_book: [
    snippet('List of dicts', 'grades = [{"name": "Ana", "score": 88}]\nprint(grades)'),
    snippet('Extract scores', 'grades = [{"score": 88}, {"score": 92}]\nscores = [g["score"] for g in grades]\nprint(scores)'),
    snippet('Average', 'print(sum([80, 90]) / 2)'),
    snippet('Max score', 'print(max([88, 92]))'),
    snippet('Mean with comp', 'grades = [{"score": 80}, {"score": 90}]\nprint(sum(g["score"] for g in grades) / len(grades))'),
  ],
  greet_module: [
    snippet('greet fn', 'def greet(name):\n    return f"Hello, {name}!"\nprint(greet("World"))'),
    snippet('main guard', 'def greet(name):\n    return f"Hello, {name}!"\n\nif __name__ == "__main__":\n    print(greet("Python"))'),
    snippet('Call twice', 'def greet(name):\n    return f"Hi, {name}"\nprint(greet("A"))\nprint(greet("B"))'),
    snippet('Return only', 'def greet(name):\n    return f"Hello, {name}!"\nprint(greet("Ada"))'),
  ],
  line_counter: [
    snippet('splitlines', 'text = "alpha\\nbeta\\n\\ngamma"\nprint(text.splitlines())'),
    snippet('strip blank', 'print(len("   ".strip()) == 0)'),
    snippet('Filter non-empty', 'lines = ["a", "", "b"]\nprint([line for line in lines if line.strip()])'),
    snippet('Count', 'text = "a\\nb\\n\\nc"\nprint(len([l for l in text.splitlines() if l.strip()]))'),
  ],
  rectangle_class: [
    snippet('class def', 'class Rectangle:\n    pass\nprint(Rectangle)'),
    snippet('__init__', 'class Rectangle:\n    def __init__(self, w, h):\n        self.width, self.height = w, h\nr = Rectangle(4, 5)\nprint(r.width)'),
    snippet('area()', 'class Rectangle:\n    def __init__(self, w, h):\n        self.width, self.height = w, h\n    def area(self):\n        return self.width * self.height\nprint(Rectangle(4, 5).area())'),
    snippet('Instance', 'class Rectangle:\n    def __init__(self, w, h):\n        self.width, self.height = w, h\n    def area(self):\n        return self.width * self.height\nprint(Rectangle(3, 3).area())'),
  ],
  contact_card: [
    snippet('Contact class', 'class Contact:\n    def __init__(self, name, email):\n        self.name, self.email = name, email\n\nc = Contact("Ana", "ana@mail.com")\nprint(c.name)'),
    snippet('label()', 'class Contact:\n    def __init__(self, n, e):\n        self.name, self.email = n, e\n    def label(self):\n        return f"{self.name} <{self.email}>"\nprint(Contact("Ana", "a@b").label())'),
    snippet('Contact list', 'book = []\nbook.append(1)\nbook.append(2)\nprint(len(book))'),
    snippet('Email check', 'print("@" in "ana@mail.com")'),
  ],
  bank_account: [
    snippet('Balance 0', 'balance = 0\nprint(balance)'),
    snippet('deposit', 'balance = 0\nbalance += 50\nprint(balance)'),
    snippet('safe withdraw', 'balance = 50\namount = 30\nif amount <= balance:\n    balance -= amount\nprint(balance)'),
    snippet('Transactions', 'transactions = 0\ntransactions += 1\ntransactions += 1\nprint(transactions)'),
    snippet('Full class', 'class BankAccount:\n    def __init__(self):\n        self.balance = 0\n    def deposit(self, amount):\n        self.balance += amount\na = BankAccount()\na.deposit(100)\nprint(a.balance)'),
  ],

  // Reused project ids with section-specific prefixes
  proj_control_flow_guess_game: [
    snippet('randint', 'import random\nprint(random.randint(1, 10))'),
    snippet('while loop', 'secret = 7\nguess = 0\nwhile guess != secret:\n    guess = 7\nprint("win")'),
    snippet('Hints', 'g, s = 3, 5\nprint("Higher" if g < s else "Lower")'),
    snippet('Attempts', 'attempts = 0\nfor _ in range(3):\n    attempts += 1\nprint(attempts)'),
  ],
  proj_control_flow_quiz_app: [
    snippet('Questions', 'questions = [{"q": "2+2?", "choices": ["3", "4"], "a": 1}]\nprint(questions)'),
    snippet('Score', 'questions = [{"a": 1}]\nanswers = [1]\nprint(sum(1 for i, a in enumerate(answers) if a == questions[i]["a"]))'),
    snippet('Percent', 'print(int(1 / 2 * 100))'),
    snippet('shuffle', 'import random\nlst = [1, 2, 3]\nrandom.shuffle(lst)\nprint(lst)'),
  ],
  proj_data_structures_quiz_app: [
    snippet('Question dict', 'q = {"choices": ["a", "b", "c"]}\nprint(len(q["choices"]))'),
    snippet('Grade', 'questions = [{"a": 1}, {"a": 0}]\nanswers = [1, 0]\nprint(sum(1 for i, a in enumerate(answers) if a == questions[i]["a"]))'),
    snippet('Shuffle', 'import random\nlst = ["a", "b", "c"]\nrandom.shuffle(lst)\nprint(lst)'),
    snippet('Percent', 'correct, total = 3, 4\nprint(int(correct / total * 100))'),
  ],
  proj_data_structures_todo_list: [
    snippet('Todo dict', 'todos = [{"id": 1, "text": "Learn", "done": False}]\nprint(todos)'),
    snippet('Append', 'todos = []\ntodos.append({"text": "Build"})\nprint(todos)'),
    snippet('Mark done', 'todos = [{"done": False}]\ntodos[0]["done"] = True\nprint(todos)'),
    snippet('json dumps', 'import json\nprint(json.dumps([{"text": "a"}]))'),
  ],
  proj_functions_unit_converter: [
    snippet('km to miles', 'def km_to_miles(km):\n    return km * 0.621371\nprint(km_to_miles(10))'),
    snippet('C to F', 'def c_to_f(c):\n    return c * 9/5 + 32\nprint(c_to_f(0))'),
    snippet('Factors dict', 'factors = {"km": 1000, "m": 1}\nprint(factors["km"])'),
    snippet('Round result', 'print(round(3.14159, 2))'),
  ],
  proj_functions_password_generator: [
    snippet('choice', 'import random\nimport string\nprint(random.choice(string.ascii_letters))'),
    snippet('Join', 'import random\nimport string\nchars = string.ascii_letters + string.digits\nprint("".join(random.choice(chars) for _ in range(8)))'),
    snippet('Function', 'import random\nimport string\n\ndef generate_password(n=12):\n    chars = string.ascii_letters + string.digits\n    return "".join(random.choice(chars) for _ in range(n))\nprint(generate_password(8))'),
    snippet('digits check', 'print(any(ch.isdigit() for ch in "a1"))'),
  ],
  proj_functions_expense_tracker: [
    snippet('Add expense', 'expenses = []\nexpenses.append({"amount": 12.5, "category": "food"})\nprint(expenses)'),
    snippet('Sum', 'print(sum(e["amount"] for e in [{"amount": 10}, {"amount": 5}]))'),
    snippet('Group by', 'totals = {}\nfor e in [{"category": "food", "amount": 10}, {"category": "food", "amount": 5}]:\n    totals[e["category"]] = totals.get(e["category"], 0) + e["amount"]\nprint(totals)'),
    snippet('Date', 'from datetime import datetime\nprint(datetime.now().strftime("%Y-%m-%d"))'),
  ],
  proj_modules_capstone_planning: [
    snippet('main guard', 'def main():\n    print("Capstone")\n\nif __name__ == "__main__":\n    main()'),
    snippet('Modules', 'modules = ["models", "cli", "utils"]\nprint(modules)'),
    snippet('Score', 'print(4 * 10 + 2 * 15)'),
    snippet('try/except', 'try:\n    int("42")\nexcept ValueError:\n    print("bad input")'),
  ],
  proj_modules_contact_book: [
    snippet('Dict store', 'contacts = {}\ncontacts["ana"] = {"phone": "555"}\nprint(contacts)'),
    snippet('get', 'contacts = {}\nprint(contacts.get("x"))'),
    snippet('lower key', 'print("BoB".lower())'),
    snippet('json', 'import json\nprint(json.dumps({"ana": {"phone": "555"}}))'),
  ],
  proj_file_handling_todo_list: [
    snippet('splitlines', 'print("a\\nb".splitlines())'),
    snippet('json load', 'import json\nprint(json.loads(\'[{"text": "a"}]\'))'),
    snippet('Todo append', 'todos = []\ntodos.append({"text": "task"})\nprint(todos)'),
    snippet('with pattern', '# with open("todos.json") as f:\n#     data = f.read()\nprint("file read")'),
  ],
  proj_file_handling_contact_book: [
    snippet('json dumps', 'import json\ncontacts = {"ana": {"phone": "555"}}\nprint(json.dumps(contacts))'),
    snippet('json loads', 'import json\nprint(json.loads(\'{"ana": {"phone": "555"}}\'))'),
    snippet('Dict contacts', 'contacts = {}\ncontacts["ana"] = {"phone": "555"}\nprint(contacts)'),
    snippet('pop delete', 'contacts = {"a": 1}\ncontacts.pop("a", None)\nprint(contacts)'),
  ],
  proj_error_handling_guess_game: [
    snippet('try int', 'try:\n    n = int("5")\nexcept ValueError:\n    n = 0\nprint(n)'),
    snippet('while loop', 'secret = 7\nguess = 7\nprint("win" if guess == secret else "lose")'),
    snippet('randint', 'import random\nprint(random.randint(1, 10))'),
    snippet('Recover input', 'try:\n    n = int("bad")\nexcept ValueError:\n    print("Invalid number")'),
  ],
  proj_error_handling_quiz_app: [
    snippet('try json', 'import json\ntry:\n    json.loads("{bad}")\nexcept json.JSONDecodeError:\n    print("bad JSON")'),
    snippet('Grade safe', 'questions = [{"a": 1}]\nanswers = [1]\nprint(sum(1 for i, a in enumerate(answers) if a == questions[i]["a"]))'),
    snippet('ValueError', 'try:\n    int("x")\nexcept ValueError:\n    print("bad int")'),
  ],
  proj_error_handling_expense_tracker: [
    snippet('try float', 'try:\n    amount = float("12.5")\nexcept ValueError:\n    amount = 0\nprint(amount)'),
    snippet('try date', 'from datetime import datetime\ntry:\n    d = datetime.strptime("2024-06-01", "%Y-%m-%d")\nexcept ValueError:\n    d = None\nprint(d)'),
    snippet('Sum expenses', 'print(sum(e["amount"] for e in [{"amount": 10}, {"amount": 5}]))'),
  ],
  proj_data_apis_weather_cli: [
    snippet('Mock API', 'data = {"city": "London", "temp_c": 18, "conditions": "Cloudy"}\nprint(data)'),
    snippet('Format', 'data = {"city": "London", "temp_c": 18, "conditions": "Cloudy", "humidity": 72}\nprint(f"{data[\'city\']}: {data[\'temp_c\']}C")'),
    snippet('get safe', 'data = {}\nprint(data.get("wind", "Unknown"))'),
    snippet('json', 'import json\nprint(json.dumps({"city": "London"}))'),
  ],
  proj_data_apis_expense_tracker: [
    snippet('Add expense', 'expenses = []\nexpenses.append({"amount": 12.5, "category": "food"})\nprint(expenses)'),
    snippet('Sum', 'print(sum(e["amount"] for e in [{"amount": 10}, {"amount": 5}]))'),
    snippet('json save', 'import json\nprint(json.dumps(expenses := [{"amount": 1}]))'),
    snippet('Filter month', 'expenses = [{"date": "2024-06-01", "amount": 1}]\nprint([e for e in expenses if e["date"].startswith("2024-06")])'),
  ],
  proj_data_apis_web_scraper: [
    snippet('Parse href', 'html = \'<a href="/page">Link</a>\'\nparts = html.split("<a href=")\nprint(parts[1].split(">")[0].strip(\'"\'))'),
    snippet('Link list', 'links = ["/page", "/about"]\nprint(links)'),
    snippet('urlopen', 'from urllib.request import urlopen\nprint("ready")'),
    snippet('sleep', 'import time\ntime.sleep(0)\nprint("rate limit")'),
  ],
}

export function sandboxSnippetsFor(levelId) {
  return SANDBOX_SNIPPETS_BY_LEVEL[levelId]
}
