import { makeTopic, snippet, ch, qMcq, qShort, pyOut, pyVar, regex, includes } from './_builders.mjs'

export const fundamentalsTopics = [
  makeTopic({
    id: 'what_is_python',
    title: 'What is Python',
    introTitle: 'Welcome to Python',
    paragraphs: [
      'Python is a high-level programming language designed to be readable and productive. Its syntax uses indentation and plain English-like keywords, which makes it one of the most popular first languages for beginners and professionals alike.',
      'Python is interpreted, meaning you can run code line by line without compiling first. It powers web apps, data science, automation, games, and artificial intelligence. The same skills you learn here transfer to many real-world projects.',
      'In CodeQuest you will write Python in the browser sandbox, solve challenges, and pass tests. Start by running the sample code below, then change it and experiment.',
    ],
    sandboxCode: 'print("Hello from Python!")\nprint(2 + 2)\nprint("Python", 3.11)',
    sandboxSnippets: [
      snippet('Hello', 'print("Hello, Python!")'),
      snippet('Math', 'print(6 * 7)'),
      snippet('Multiple values', 'print("Python", 3.11)'),
      snippet('Power', 'print(2 ** 10)'),
      snippet('Three lines', 'print("Alex")\nprint(12)\nprint("Ready to code!")'),
    ],
    readMore: { label: 'About Python', url: 'https://www.python.org/about/', source: 'Python Software Foundation' },
    challenges: [
      ch('easy', 'Print your name', 'Print your first name as text.', pyOut('Alex'), { starterCode: 'print(' }),
      ch('easy', 'Print a number', 'Print the number `100` on its own line.', pyOut('100'), { starterCode: 'print(' }),
      ch('easy', 'Two print lines', 'Print `Line 1` then `Line 2` on separate lines.', pyOut('Line 1\nLine 2'), { starterCode: 'print(' }),
      ch('medium', 'Print a sentence', 'Print exactly: `I am learning Python.`', pyOut('I am learning Python.'), { starterCode: 'print(' }),
      ch('medium', 'Math in print', 'Print the result of `6 * 7` using print.', pyOut('42'), { starterCode: 'print(' }),
      ch('medium', 'Multiple values', 'Use one print with two arguments to output `Hello World` (space between).', pyOut('Hello World'), { starterCode: 'print(' }),
      ch('hard', 'Power expression', 'Print `2` to the power of `10` (1024).', pyOut('1024'), { starterCode: 'print(' }),
      ch('hard', 'Combine text and math', 'Print `The answer is 42` using string and number in one print call.', pyOut('The answer is 42'), { starterCode: 'print(' }),
      ch('expert', 'Three-line program', 'Print three lines: your name, your age as a number, and `Ready to code!`', includes('print'), { starterCode: '' }),
      ch('expert', 'Formatted output', 'Print `Score: 95/100` exactly.', pyOut('Score: 95/100'), { starterCode: 'print(' }),
    ],
    questions: [
      qMcq('What best describes Python?', [{ id: 'a', label: 'A compiled language only for browsers' }, { id: 'b', label: 'A readable, high-level programming language' }, { id: 'c', label: 'A database system' }], 'b'),
      qShort('What function displays output to the console in Python?', 'print'),
      qMcq('Python code blocks are defined mainly by:', [{ id: 'a', label: 'Curly braces {}' }, { id: 'b', label: 'Indentation' }, { id: 'c', label: 'Semicolons only' }], 'b'),
      qShort('Is Python typically compiled to machine code before every run? (yes/no)', 'no'),
      qMcq('Which is valid Python output code?', [{ id: 'a', label: 'echo("hi")' }, { id: 'b', label: 'print("hi")' }, { id: 'c', label: 'console.log("hi")' }], 'b'),
      qMcq('Python is widely used for:', [{ id: 'a', label: 'Only hardware drivers' }, { id: 'b', label: 'Web, data, automation, and AI' }, { id: 'c', label: 'Only spreadsheets' }], 'b'),
      qShort('Type the symbol used to call a function in Python after its name.', '('),
      qMcq('Running Python in CodeQuest sandbox means:', [{ id: 'a', label: 'Code runs in your browser via WebAssembly' }, { id: 'b', label: 'Code is sent to a remote server always' }, { id: 'c', label: 'Python cannot run in browser' }], 'a'),
      qMcq('Expert: Which prints multiple items with a space by default?', [{ id: 'a', label: 'print(a, b)' }, { id: 'b', label: 'print[a, b]' }, { id: 'c', label: 'print{a, b}' }], 'a'),
      qShort('Expert: What operator raises a number to a power in Python?', '**'),
    ],
  }),

  makeTopic({
    id: 'print_command',
    title: 'Print command',
    introTitle: 'Output with print()',
    paragraphs: [
      'The `print()` function sends text and values to the console. It is your first debugging tool and the simplest way to see what your program is doing.',
      'You can print string literals in quotes, variables, and expressions. `print("Hi")`, `print(name)`, and `print(3 + 4)` are all valid. By default, multiple arguments are separated by a space.',
      'Use `sep` and `end` keyword arguments for finer control: `print(1, 2, sep="-")` prints `1-2`, and `end=""` avoids the trailing newline.',
    ],
    sandboxCode: 'name = "Sam"\nprint("Hello,", name)\nprint(10, 20, 30, sep=" | ")\nprint("no newline", end="")\nprint(" continued")',
    sandboxSnippets: [
      snippet('Exact text', 'print("Hello, CodeQuest!")'),
      snippet('Print variable', 'x = 5\nprint(x)'),
      snippet('sep argument', 'print("a", "b", "c", sep=",")'),
      snippet('f-string', 'score = 88\nprint(f"Score: {score}")'),
      snippet('end argument', 'print("Loading", end="")\nprint("...")'),
    ],
    readMore: { label: 'Python tutorial — output', url: 'https://docs.python.org/3/tutorial/inputoutput.html', source: 'Python docs' },
    challenges: [
      ch('easy', 'Hello CodeQuest', 'Print exactly `Hello, CodeQuest!`', pyOut('Hello, CodeQuest!'), { starterCode: 'print(' }),
      ch('easy', 'Print a variable', 'Given `x = 5`, print `x`.', pyVar('x', 'x == 5', 'x = 5'), { starterCode: 'print(' }),
      ch('easy', 'Print boolean', 'Print `True`.', pyOut('True'), { starterCode: 'print(' }),
      ch('medium', 'sep argument', 'Print `a`, `b`, `c` separated by commas using sep.', pyOut('a,b,c'), { starterCode: 'print(' }),
      ch('medium', 'Expression print', 'Print the result of `15 - 6`.', pyOut('9'), { starterCode: 'print(' }),
      ch('medium', 'Two variables', 'Given `first="Ana"` and `last="Lee"`, print both with default spacing.', pyOut('Ana Lee'), { starterCode: 'first = "Ana"\nlast = "Lee"\nprint(' }),
      ch('hard', 'end argument', 'Print `Loading` without newline then `...` on same line conceptually; final line should read `Loading...`', pyOut('Loading...'), { starterCode: 'print(' }),
      ch('hard', 'f-string print', 'Given `score=88`, print `Score: 88` using an f-string.', pyOut('Score: 88'), { starterCode: 'score = 88\nprint(' }),
      ch('expert', 'Multi-line output', 'Print exactly two lines: `First` and `Second`.', pyOut('First\nSecond'), { starterCode: '' }),
      ch('expert', 'repr vs str', 'Print the string `O\'Clock` with quotes visible using repr in print.', includes('repr'), { starterCode: 's = "O\'Clock"\n' }),
    ],
    questions: [
      qMcq('print("a", "b") outputs:', [{ id: 'a', label: 'ab' }, { id: 'b', label: 'a b' }, { id: 'c', label: 'a,b' }], 'b'),
      qShort('Which keyword argument changes the separator between printed items?', 'sep'),
      qMcq('print(3) displays:', [{ id: 'a', label: 'The integer 3 as text' }, { id: 'b', label: 'Nothing' }, { id: 'c', label: 'An error always' }], 'a'),
      qShort('Type the keyword argument that controls the trailing character after print.', 'end'),
      qMcq('Which prints a variable value?', [{ id: 'a', label: 'print(x)' }, { id: 'b', label: 'print["x"]' }, { id: 'c', label: 'echo x' }], 'a'),
      qMcq('f"Hi {name}" inside print is called:', [{ id: 'a', label: 'An f-string' }, { id: 'b', label: 'A tuple' }, { id: 'c', label: 'A decorator' }], 'a'),
      qShort('What character ends a string in Python? (single or double quote name)', 'quote'),
      qMcq('print() with no arguments prints:', [{ id: 'a', label: 'A blank line' }, { id: 'b', label: 'Syntax error' }, { id: 'c', label: 'None' }], 'a'),
      qMcq('Expert: sep="," with print(1,2) gives:', [{ id: 'a', label: '1,2' }, { id: 'b', label: '1 2' }, { id: 'c', label: '12' }], 'a'),
      qShort('Expert: repr() shows a value in a form useful for:', 'debugging'),
    ],
  }),

  makeTopic({
    id: 'variables',
    title: 'Variables',
    introTitle: 'Names for values',
    paragraphs: [
      'Variables are names that refer to values in memory. You create one with the assignment operator `=`: `age = 21` stores 21 under the name `age`.',
      'Python is dynamically typed: a variable can refer to different types over time. Use descriptive snake_case names like `user_count` and `total_price`.',
      'You can reassign variables, combine them in expressions, and pass them to functions. Good naming makes programs easier to read and maintain.',
    ],
    sandboxCode: 'count = 0\ncount = count + 1\nname = "Riley"\nactive = True\nprint(name, count, active)',
    sandboxSnippets: [
      snippet('Assign int', 'answer = 42'),
      snippet('Assign string', 'greeting = "hello"'),
      snippet('Boolean', 'is_ready = True'),
      snippet('Increment', 'count = 5\ncount = count + 1\nprint(count)'),
      snippet('Multiple assign', 'x, y = 10, 20\nprint(x, y)'),
    ],
    readMore: { label: 'First steps', url: 'https://docs.python.org/3/tutorial/introduction.html#first-steps-towards-programming', source: 'Python docs' },
    challenges: [
      ch('easy', 'Assign integer', 'Assign `42` to `answer`.', pyVar('answer', 'answer == 42'), { starterCode: 'answer = ' }),
      ch('easy', 'Assign string', 'Assign `hello` to `greeting`.', pyVar('greeting', "greeting == 'hello'"), { starterCode: 'greeting = ' }),
      ch('easy', 'Boolean variable', 'Set `is_ready` to `True`.', regex('^\\s*is_ready\\s*=\\s*True\\s*$'), { starterCode: 'is_ready = ' }),
      ch('medium', 'Reassign', 'Given `n=1`, write one line to make `n` equal `2`.', regex('^\\s*n\\s*=\\s*2\\s*$'), { starterCode: 'n = 1\n' }),
      ch('medium', 'Increment', 'Given `count=5`, add 1 using `count = count + 1`.', pyVar('count', 'count == 6', 'count = 5'), { starterCode: '' }),
      ch('medium', 'Swap via temp', 'Given `a=1` and `b=2`, swap values using a third variable.', pyVar('a', 'a == 2 and b == 1', 'a = 1\nb = 2'), { starterCode: '' }),
      ch('hard', 'Multiple assignment', 'Assign `x=10` and `y=20` in one line.', regex('x\\s*=\\s*10.*y\\s*=\\s*20|y\\s*=\\s*20.*x\\s*=\\s*10'), { starterCode: '' }),
      ch('hard', 'Snake case name', 'Create `total_score = 100`.', pyVar('total_score', 'total_score == 100'), { starterCode: '' }),
      ch('expert', 'Chain assignment', 'Set `a`, `b`, and `c` all to `0` in one statement.', pyVar('c', 'a == 0 and b == 0 and c == 0'), { starterCode: '' }),
      ch('expert', 'Update from expression', 'Given `price=9.5` and `tax_rate=0.1`, set `total` to price plus tax.', pyVar('total', 'abs(total - 10.45) < 0.01', 'price = 9.5\ntax_rate = 0.1'), { starterCode: 'total = ' }),
    ],
    questions: [
      qMcq('Which assigns 3 to x?', [{ id: 'a', label: 'x := 3' }, { id: 'b', label: 'x = 3' }, { id: 'c', label: '3 = x' }], 'b'),
      qShort('What naming style does PEP 8 recommend for variables?', 'snake_case'),
      qMcq('True is a:', [{ id: 'a', label: 'bool' }, { id: 'b', label: 'int' }, { id: 'c', label: 'str' }], 'a'),
      qShort('Can you reassign a variable to a different type in Python? (yes/no)', 'yes'),
      qMcq('Invalid variable name:', [{ id: 'a', label: 'user_age' }, { id: 'b', label: '2fast' }, { id: 'c', label: '_hidden' }], 'b'),
      qMcq('count = count + 1 is:', [{ id: 'a', label: 'Reassignment' }, { id: 'b', label: 'Function definition' }, { id: 'c', label: 'Import' }], 'a'),
      qShort('Assignment operator symbol?', '='),
      qMcq('a = b = 0 sets:', [{ id: 'a', label: 'Both a and b to 0' }, { id: 'b', label: 'Only a to 0' }, { id: 'c', label: 'Syntax error' }], 'a'),
      qMcq('Expert: Which is a valid name?', [{ id: 'a', label: 'my-var' }, { id: 'b', label: 'my_var' }, { id: 'c', label: 'class' }], 'b'),
      qShort('Expert: Dynamic typing means types are checked at:', 'runtime'),
    ],
  }),

  makeTopic({
    id: 'operators',
    title: 'Operators',
    introTitle: 'Math and comparisons',
    paragraphs: [
      'Python provides arithmetic operators: `+`, `-`, `*`, `/` (float division), `//` (floor division), `%` (modulo), and `**` (power). Operator precedence follows standard math rules.',
      'Comparison operators return booleans: `==`, `!=`, `<`, `>`, `<=`, `>=`. Logical operators `and`, `or`, and `not` combine conditions.',
      'Assignment shortcuts like `+=` and `*=` update variables in place. Understanding operators is essential for control flow and loops.',
    ],
    sandboxCode: 'a, b = 10, 3\nprint(a + b, a // b, a % b, a ** 2)\nprint(a > b, a == 10, not False)',
    sandboxSnippets: [
      snippet('Floor div', 'q = 17 // 5\nprint(q)'),
      snippet('Power', 'p = 2 ** 8\nprint(p)'),
      snippet('Modulo', 'print(10 % 3)'),
      snippet('Logical and', 'result = True and False\nprint(result)'),
      snippet('Chained compare', 'ok = 1 < 2 < 3\nprint(ok)'),
    ],
    readMore: { label: 'Operators', url: 'https://docs.python.org/3/reference/expressions.html#operator-precedence', source: 'Python docs' },
    challenges: [
      ch('easy', 'Addition', 'Write expression `10 + 5`.', includes('10', '+', '5'), { starterCode: '' }),
      ch('easy', 'Comparison', 'Write `7 < 3`.', regex('7\\s*<\\s*3'), { starterCode: '' }),
      ch('easy', 'Modulo', 'Write `10 % 3`.', includes('%'), { starterCode: '' }),
      ch('medium', 'Floor division', 'Assign `q = 17 // 5`.', pyVar('q', 'q == 3'), { starterCode: 'q = ' }),
      ch('medium', 'Power', 'Assign `p = 2 ** 8`.', pyVar('p', 'p == 256'), { starterCode: 'p = ' }),
      ch('medium', 'Logical and', 'Write expression True and False.', pyVar('result', 'result == False', 'result = True and False'), { starterCode: 'result = ' }),
      ch('hard', 'Chained comparison', 'Write `1 < 2 < 3` (should be True).', pyVar('ok', 'ok == True', 'ok = 1 < 2 < 3'), { starterCode: '' }),
      ch('hard', 'Augmented assign', 'Given `x=5`, use `+=` to make x equal 8.', pyVar('x', 'x == 8', 'x = 5'), { starterCode: '' }),
      ch('expert', 'Mixed precedence', 'Evaluate `2 + 3 * 4` into variable `val`.', pyVar('val', 'val == 14'), { starterCode: 'val = ' }),
      ch('expert', 'not operator', 'Set `flag = not True`.', pyVar('flag', 'flag == False'), { starterCode: 'flag = ' }),
    ],
    questions: [
      qMcq('`//` is:', [{ id: 'a', label: 'Floor division' }, { id: 'b', label: 'Comment' }, { id: 'c', label: 'Power' }], 'a'),
      qShort('Operator for exponentiation?', '**'),
      qMcq('`10 / 4` in Python 3 equals:', [{ id: 'a', label: '2.5' }, { id: 'b', label: '2' }, { id: 'c', label: '3' }], 'a'),
      qShort('Operator for remainder?', '%'),
      qMcq('`==` tests:', [{ id: 'a', label: 'Equality' }, { id: 'b', label: 'Assignment' }, { id: 'c', label: 'Greater than' }], 'a'),
      qMcq('not True is:', [{ id: 'a', label: 'False' }, { id: 'b', label: 'True' }, { id: 'c', label: 'None' }], 'a'),
      qShort('Logical operator for both conditions true?', 'and'),
      qMcq('2 + 3 * 4 equals:', [{ id: 'a', label: '14' }, { id: 'b', label: '20' }, { id: 'c', label: '24' }], 'a'),
      qMcq('Expert: `+=` is:', [{ id: 'a', label: 'Augmented assignment' }, { id: 'b', label: 'String concat only' }, { id: 'c', label: 'Invalid' }], 'a'),
      qShort('Expert: `!=` means:', 'not equal'),
    ],
  }),

  makeTopic({
    id: 'data_types',
    title: 'Data Types',
    introTitle: 'Built-in types',
    paragraphs: [
      'Python has several core built-in types: `int` for integers, `float` for decimals, `str` for text, `bool` for True/False, and `None` for absence of value.',
      'Use `type()` to inspect what a value is. Types determine what operations are allowed — you can add numbers but not generally add a number and a string without conversion.',
      'Python is dynamically typed, but values still have types at runtime. Learning to recognize types prevents many common errors.',
    ],
    sandboxCode: 'values = [42, 3.14, "hi", True, None]\nfor v in values:\n    print(v, type(v).__name__)',
    sandboxSnippets: [
      snippet('type()', 'print(type(3), type(3.14), type("hi"))'),
      snippet('None', 'data = None\nprint(data)'),
      snippet('bool from compare', 'ok = 5 > 2\nprint(ok)'),
      snippet('isinstance', 'flag = isinstance(3.0, float)\nprint(flag)'),
      snippet('Float division', 'r = 7 / 2\nprint(r, type(r).__name__)'),
    ],
    challenges: [
      ch('easy', 'Integer literal', 'Assign int `7` to `n`.', pyVar('n', 'type(n) == int'), { starterCode: 'n = ' }),
      ch('easy', 'Float literal', 'Assign `3.5` to `x`.', pyVar('x', 'type(x) == float'), { starterCode: 'x = ' }),
      ch('easy', 'String type', 'Assign `"ok"` to `s`.', pyVar('s', 'type(s) == str'), { starterCode: 's = ' }),
      ch('medium', 'type() check', 'Given `v=10`, set `t` to `type(v)`.', pyVar('t', 't == int', 'v = 10'), { starterCode: 't = ' }),
      ch('medium', 'None assignment', 'Set `data = None`.', pyVar('data', 'data is None'), { starterCode: 'data = ' }),
      ch('medium', 'bool from comparison', 'Set `ok = (5 > 2)`.', pyVar('ok', 'ok == True'), { starterCode: 'ok = ' }),
      ch('hard', 'isinstance check', 'Check if `3.0` is float using isinstance into `flag`.', pyVar('flag', 'flag == True', ''), { starterCode: 'flag = isinstance(3.0, float)' }),
      ch('hard', 'Mixed types list', 'Create list `[1, "a", True]` assigned to `mix`.', pyVar('mix', 'len(mix) == 3'), { starterCode: 'mix = ' }),
      ch('expert', 'Type of expression', 'Store type of `2 ** 3` in `t`.', pyVar('t', 't == int'), { starterCode: 't = type(' }),
      ch('expert', 'Float from division', 'Assign `7 / 2` to `r` and confirm float.', pyVar('r', 'type(r) == float'), { starterCode: 'r = ' }),
    ],
    questions: [
      qMcq('type(3) is:', [{ id: 'a', label: 'int' }, { id: 'b', label: 'float' }, { id: 'c', label: 'str' }], 'a'),
      qShort('Type of True?', 'bool'),
      qMcq('None represents:', [{ id: 'a', label: 'No value / null' }, { id: 'b', label: 'Zero' }, { id: 'c', label: 'Empty string' }], 'a'),
      qShort('Function to inspect type?', 'type'),
      qMcq('3.0 is a:', [{ id: 'a', label: 'float' }, { id: 'b', label: 'int' }, { id: 'c', label: 'bool' }], 'a'),
      qMcq('"42" is a:', [{ id: 'a', label: 'str' }, { id: 'b', label: 'int' }, { id: 'c', label: 'list' }], 'a'),
      qShort('Literal for empty value keyword?', 'None'),
      qMcq('isinstance(5, int) returns:', [{ id: 'a', label: 'True' }, { id: 'b', label: 'False' }, { id: 'c', label: '5' }], 'a'),
      qMcq('Expert: type(7/2) is:', [{ id: 'a', label: 'float' }, { id: 'b', label: 'int' }, { id: 'c', label: 'str' }], 'a'),
      qShort('Expert: bool is subclass of:', 'int'),
    ],
  }),

  makeTopic({
    id: 'strings',
    title: 'Strings',
    introTitle: 'Working with text',
    paragraphs: [
      'Strings (`str`) represent text. You can create them with single or double quotes: `"hello"` and `\'hello\'` are equivalent. Triple quotes allow multi-line strings.',
      'Common operations include concatenation (`+`), repetition (`*`), slicing (`s[0]`), and methods like `.upper()`, `.strip()`, and `.replace()`. f-strings embed expressions: `f"Hi {name}"`.',
      'Strings are immutable: methods return new strings rather than changing the original. Indexing starts at 0; negative indices count from the end.',
    ],
    sandboxCode: 'name = "python"\nprint(name.upper(), len(name))\nprint(name[0], name[-1])\nprint(f"Learning {name}!")',
    sandboxSnippets: [
      snippet('upper()', 'loud = "hi".upper()\nprint(loud)'),
      snippet('len()', 's = "abc"\nprint(len(s))'),
      snippet('Slice', 'word = "code"\nprint(word[0], word[-1])'),
      snippet('f-string', 'n = 3\nprint(f"Count: {n}")'),
      snippet('strip()', 't = "  hi  ".strip()\nprint(t)'),
    ],
    challenges: [
      ch('easy', 'Create string', 'Assign `"cat"` to `pet`.', pyVar('pet', "pet == 'cat'"), { starterCode: 'pet = ' }),
      ch('easy', 'Concatenate', 'Set `msg` to `"Hello" + "!"`.', pyVar('msg', "msg == 'Hello!'"), { starterCode: 'msg = ' }),
      ch('easy', 'len()', 'Given `s="abc"`, set `n = len(s)`.', pyVar('n', 'n == 3', 's = "abc"'), { starterCode: '' }),
      ch('medium', 'upper()', 'Uppercase `"hi"` into `loud`.', pyVar('loud', "loud == 'HI'"), { starterCode: 'loud = ' }),
      ch('medium', 'Slice first char', 'Given `word="code"`, set `first = word[0]`.', pyVar('first', "first == 'c'", 'word = "code"'), { starterCode: '' }),
      ch('medium', 'f-string', 'Given `n=3`, set `line = f"Count: {n}"`.', pyVar('line', "line == 'Count: 3'"), { starterCode: 'n = 3\nline = ' }),
      ch('hard', 'strip()', 'Strip spaces from `"  hi  "` into `t`.', pyVar('t', "t == 'hi'"), { starterCode: 't = ' }),
      ch('hard', 'replace()', 'Replace `a` with `o` in `"cat"` → `cot`.', pyVar('out', "out == 'cot'"), { starterCode: 'out = ' }),
      ch('expert', 'Negative index', 'Get last char of `"Python"` into `last`.', pyVar('last', "last == 'n'"), { starterCode: 'last = ' }),
      ch('expert', 'split words', 'Split `"a b c"` into list of 3 parts.', pyVar('parts', 'parts == ["a","b","c"]'), { starterCode: 'parts = ' }),
    ],
    questions: [
      qMcq('Strings are:', [{ id: 'a', label: 'Immutable' }, { id: 'b', label: 'Mutable lists' }, { id: 'c', label: 'Always bytes' }], 'a'),
      qShort('Index of first character?', '0'),
      qMcq('"a" + "b" gives:', [{ id: 'a', label: 'ab' }, { id: 'b', label: 'a b' }, { id: 'c', label: 'Error' }], 'a'),
      qShort('Method to remove surrounding whitespace?', 'strip'),
      qMcq('f"{x}" requires:', [{ id: 'a', label: 'f prefix on string' }, { id: 'b', label: 'g prefix' }, { id: 'c', label: 'No quotes' }], 'a'),
      qMcq('len("hi") is:', [{ id: 'a', label: '2' }, { id: 'b', label: '3' }, { id: 'c', label: '1' }], 'a'),
      qShort('Quote type for multi-line strings (three of same)?', 'triple'),
      qMcq('s[-1] gets:', [{ id: 'a', label: 'Last character' }, { id: 'b', label: 'First character' }, { id: 'c', label: 'Length' }], 'a'),
      qMcq('Expert: "x" * 3 is:', [{ id: 'a', label: 'xxx' }, { id: 'b', label: 'x3' }, { id: 'c', label: 'Error' }], 'a'),
      qShort('Expert: split() returns a:', 'list'),
    ],
  }),

  makeTopic({
    id: 'input_output',
    title: 'Input and Output',
    introTitle: 'Talking to the user',
    paragraphs: [
      'Programs often read input with `input("prompt")`, which always returns a string. Combine with `print()` for interactive command-line style programs.',
      'Convert input before math: `int(input("Age? "))`. Always validate or handle bad input in real apps; here we focus on the basics.',
      'In the browser sandbox, `input()` may be simulated or limited; still learn the pattern for local Python and projects.',
    ],
    sandboxCode: '# Simulated input pattern\nuser = "42"  # pretend input returned "42"\nprint("You entered:", user)\nprint("Double:", int(user) * 2)',
    sandboxSnippets: [
      snippet('Parse int', 'n = int("12")\nprint(n)'),
      snippet('input pattern', 'name = input("Name? ")\nprint(name)'),
      snippet('Convert float', 'x = float("3.5")\nprint(x)'),
      snippet('Simulated input', 'user = "42"\nprint("Double:", int(user) * 2)'),
      snippet('strip input', 'raw = "  5  "\nprint(int(raw.strip()))'),
    ],
    challenges: [
      ch('easy', 'Print prompt result', 'Print `Enter name:` (simulating a prompt label).', pyOut('Enter name:'), { starterCode: 'print(' }),
      ch('easy', 'Parse int string', 'Convert `"12"` to int in `n`.', pyVar('n', 'n == 12'), { starterCode: 'n = int(' }),
      ch('medium', 'input pattern', 'Write a line assigning `name = input("Name? ")`.', regex('name\\s*=\\s*input\\('), { starterCode: '' }),
      ch('medium', 'Convert float', 'Convert `"3.5"` to float in `x`.', pyVar('x', 'x == 3.5'), { starterCode: 'x = float(' }),
      ch('hard', 'Print formatted input', 'Given `name="Kim"`, print `Hello, Kim!`', pyOut('Hello, Kim!'), { starterCode: 'name = "Kim"\nprint(' }),
      ch('hard', 'int from input var', 'Given `raw="8"`, set `n = int(raw)`.', pyVar('n', 'n == 8', 'raw = "8"'), { starterCode: '' }),
      ch('expert', 'Combined prompt', 'Print `Result: 15` after computing int("7")+int("8").', pyOut('Result: 15'), { starterCode: '' }),
      ch('expert', 'strip input', 'Given `raw="  5  "`, int after strip.', pyVar('val', 'val == 5', 'raw = "  5  "'), { starterCode: 'val = int(raw.strip())' }),
      ch('expert', 'Multi-value display', 'Print two lines from variables `a=1`, `b=2`.', pyOut('1\n2'), { starterCode: 'a,b=1,2\n' }),
      ch('expert', 'input returns str', 'Show you know input type: assign str to `t = type(input())` pattern using comment only — set `note = "str"`.', pyVar('note', "note == 'str'"), { starterCode: 'note = ' }),
    ],
    questions: [
      qMcq('input() return type:', [{ id: 'a', label: 'str' }, { id: 'b', label: 'int' }, { id: 'c', label: 'bool' }], 'a'),
      qShort('Function to read user text?', 'input'),
      qMcq('int("9") is:', [{ id: 'a', label: '9' }, { id: 'b', label: '"9"' }, { id: 'c', label: 'Error always' }], 'a'),
      qShort('Convert string to float function?', 'float'),
      qMcq('print before input is used to:', [{ id: 'a', label: 'Show prompts/messages' }, { id: 'b', label: 'Delete files' }, { id: 'c', label: 'Compile code' }], 'a'),
      qMcq('Safe pattern for numeric input:', [{ id: 'a', label: 'int(input(...))' }, { id: 'b', label: 'input + 1 without convert' }, { id: 'c', label: 'bool(input())' }], 'a'),
      qShort('Strip removes extra:', 'whitespace'),
      qMcq('Expert: input always needs conversion for:', [{ id: 'a', label: 'Math on numbers' }, { id: 'b', label: 'Printing text' }, { id: 'c', label: 'Comments' }], 'a'),
      qMcq('Expert: float("2.5") is:', [{ id: 'a', label: '2.5' }, { id: 'b', label: '2' }, { id: 'c', label: 'str' }], 'a'),
      qShort('Expert: Interactive programs combine input and:', 'output'),
    ],
  }),

  makeTopic({
    id: 'comments_style',
    title: 'Comments and Style',
    introTitle: 'Readable code',
    paragraphs: [
      'Comments start with `#` and are ignored by Python. Use them to explain why, not what obvious code does. Docstrings (`"""..."""`) document modules, classes, and functions.',
      'PEP 8 is Python\'s style guide: 4-space indentation, snake_case for variables and functions, blank lines between logical sections, and lines ideally under 79–99 characters.',
      'Readable code reduces bugs and helps collaborators. Consistent style matters as much as correct syntax in real projects.',
    ],
    sandboxCode: '# Greet the user\nname = "Dev"  # short variable ok in sandbox\n\ndef greet(person):\n    """Return a greeting string."""\n    return f"Hello, {person}!"\n\nprint(greet(name))',
    sandboxSnippets: [
      snippet('Comment', '# learning python'),
      snippet('Inline comment', 'x = 1  # start value'),
      snippet('snake_case', 'user_name = "Alex"\nprint(user_name)'),
      snippet('Docstring', 'def add(a, b):\n    """Add two numbers."""\n    return a + b\n\nprint(add(2, 3))'),
      snippet('PEP 8 spacing', 'y = 2\nprint(y)'),
    ],
    challenges: [
      ch('easy', 'Add comment', 'Write a comment line `# learning python`.', regex('^\\s*#\\s*learning python'), { starterCode: '' }),
      ch('easy', 'Inline comment', 'After `x = 1` add comment `# start`.', regex('x\\s*=\\s*1.*#\\s*start'), { starterCode: 'x = 1 ' }),
      ch('medium', 'Docstring', 'Define function with docstring `"""add two"""`.', includes('"""', 'def'), { starterCode: 'def add(a, b):\n    ' }),
      ch('medium', 'snake_case var', 'Use variable `user_name = "a"`.', pyVar('user_name', "user_name == 'a'"), { starterCode: '' }),
      ch('hard', 'PEP8 spaces', 'Assign with spaces: `y = 2`.', regex('^\\s*y\\s*=\\s*2\\s*$'), { starterCode: '' }),
      ch('hard', 'Block comment', 'Two comment lines explaining steps.', regex('#'), { starterCode: '# step 1\n# step 2\n' }),
      ch('expert', 'Function naming', 'Define `def calculate_total(): pass`', includes('def calculate_total'), { starterCode: '' }),
      ch('expert', 'Readable names', 'Avoid `x`; use `item_count = 3`.', pyVar('item_count', 'item_count == 3'), { starterCode: '' }),
      ch('expert', 'Docstring access', 'Function with docstring; assign `d = fn.__doc__` not required — use `msg = "doc"` if fn has doc.', includes('"""'), { starterCode: 'def fn():\n    """help"""\n    pass\n' }),
      ch('expert', 'Style summary', 'Set `style = "pep8"` to show you recall the guide name.', pyVar('style', "style == 'pep8'"), { starterCode: 'style = ' }),
    ],
    questions: [
      qMcq('Comments begin with:', [{ id: 'a', label: '#' }, { id: 'b', label: '//' }, { id: 'c', label: '--' }], 'a'),
      qShort('PEP 8 recommends indent size in spaces?', '4'),
      qMcq('Docstrings use:', [{ id: 'a', label: 'Triple quotes' }, { id: 'b', label: 'Only #' }, { id: 'c', label: 'HTML tags' }], 'a'),
      qShort('Variable naming style in PEP 8?', 'snake_case'),
      qMcq('Good comments explain:', [{ id: 'a', label: 'Why' }, { id: 'b', label: 'Every character' }, { id: 'c', label: 'Binary only' }], 'a'),
      qMcq('Function names should be:', [{ id: 'a', label: 'lowercase_with_underscores' }, { id: 'b', label: 'ALLCAPS' }, { id: 'c', label: 'with spaces' }], 'a'),
      qShort('Are comments executed? (yes/no)', 'no'),
      qMcq('Expert: Class names in PEP 8 use:', [{ id: 'a', label: 'CapWords' }, { id: 'b', label: 'snake_case' }, { id: 'c', label: 'kebab-case' }], 'a'),
      qMcq('Expert: """ at start of function is:', [{ id: 'a', label: 'Docstring' }, { id: 'b', label: 'String ignored always' }, { id: 'c', label: 'Syntax error' }], 'a'),
      qShort('Expert: Readability counts — who said it in Zen of Python spirit?', 'tim peters'),
    ],
  }),

  makeTopic({
    id: 'type_conversion',
    title: 'Type Conversion',
    introTitle: 'Casting between types',
    paragraphs: [
      'Convert values with constructors: `int()`, `float()`, `str()`, and `bool()`. `int("42")` → 42; `str(42)` → `"42"`.',
      'Not all conversions succeed: `int("hi")` raises ValueError. Always validate user input in production code.',
      'Truthy/falsy rules: `0`, `""`, `[]`, and `None` are falsy; most other values are truthy. `bool(1)` is True.',
    ],
    sandboxCode: 'print(int("10") + 5)\nprint(str(3.14))\nprint(bool(0), bool("0"))\nprint(float(7))',
    sandboxSnippets: [
      snippet('str to int', 'n = int("8")\nprint(n)'),
      snippet('int to str', 's = str(99)\nprint(s, type(s).__name__)'),
      snippet('bool()', 'print(bool(0), bool(""))'),
      snippet('float()', 'x = float("2.5")\nprint(x)'),
      snippet('Chained convert', 's = str(int("7"))\nprint(s)'),
    ],
    challenges: [
      ch('easy', 'str to int', 'int("8") into `n`.', pyVar('n', 'n == 8'), { starterCode: 'n = int(' }),
      ch('easy', 'int to str', 'str(99) into `s`.', pyVar('s', "s == '99'"), { starterCode: 's = str(' }),
      ch('medium', 'float convert', 'float("2.5") into `x`.', pyVar('x', 'x == 2.5'), { starterCode: 'x = float(' }),
      ch('medium', 'bool convert', 'bool(0) into `f`.', pyVar('f', 'f == False'), { starterCode: 'f = bool(' }),
      ch('hard', 'chained convert', 'str(int("7")) into `s` should be "7".', pyVar('s', "s == '7'"), { starterCode: 's = ' }),
      ch('hard', 'int truncates float?', 'int(3.9) into `n`.', pyVar('n', 'n == 3'), { starterCode: 'n = int(3.9)' }),
      ch('expert', 'truthy string', 'bool("") vs bool("a") — set `empty=False, has=True`.', pyVar('empty', 'empty == False and has == True', 'has = bool("a")\nempty = bool("")'), { starterCode: '' }),
      ch('expert', 'round vs int', 'round(3.6) into `r`.', pyVar('r', 'r == 4'), { starterCode: 'r = round(3.6)' }),
      ch('expert', 'hex string', 'int("ff", 16) into `n`.', pyVar('n', 'n == 255'), { starterCode: 'n = ' }),
      ch('expert', 'str of bool', 'str(True) into `s`.', pyVar('s', "s == 'True'"), { starterCode: 's = str(True)' }),
    ],
    questions: [
      qMcq('int("5") is:', [{ id: 'a', label: '5' }, { id: 'b', label: '"5"' }, { id: 'c', label: 'Error always' }], 'a'),
      qShort('Convert number to text function?', 'str'),
      qMcq('bool(0) is:', [{ id: 'a', label: 'False' }, { id: 'b', label: 'True' }, { id: 'c', label: '0' }], 'a'),
      qShort('float("1.2") produces type?', 'float'),
      qMcq('int(3.9) truncates toward:', [{ id: 'a', label: 'Zero (3)' }, { id: 'b', label: '4' }, { id: 'c', label: '3.9' }], 'a'),
      qMcq('Falsy value:', [{ id: 'a', label: '[]' }, { id: 'b', label: '[0]' }, { id: 'c', label: '"0"' }], 'a'),
      qShort('Invalid int("x") raises:', 'ValueError'),
      qMcq('str(True) is:', [{ id: 'a', label: '"True"' }, { id: 'b', label: 'True' }, { id: 'c', label: '1' }], 'a'),
      qMcq('Expert: int("ff", 16) is:', [{ id: 'a', label: '255' }, { id: 'b', label: 'ff' }, { id: 'c', label: '16' }], 'a'),
      qShort('Expert: bool("False") is:', 'True'),
    ],
  }),

  makeTopic({
    id: 'numbers_math',
    title: 'Numbers and Math',
    introTitle: 'Numeric computing',
    paragraphs: [
      'Python integers have unlimited precision in theory; floats follow IEEE double precision. Use `math` module for sqrt, floor, ceil, and trigonometry.',
      'The `decimal` and `fractions` modules help with exact arithmetic when floats are not enough. For learning, built-in operators cover most needs.',
      'Remember `/` always returns float in Python 3; use `//` for integer division when you need whole numbers.',
    ],
    sandboxCode: 'import math\nprint(abs(-5), round(2.675, 2))\nprint(math.sqrt(16), math.pi)\nprint(0.1 + 0.2)  # float surprise',
    sandboxSnippets: [
      snippet('abs()', 'print(abs(-7))'),
      snippet('round()', 'print(round(4.6))'),
      snippet('min / max', 'print(max(1, 9, 3))'),
      snippet('math.sqrt', 'import math\nprint(math.sqrt(25))'),
      snippet('divmod', 'q, rem = divmod(17, 5)\nprint(q, rem)'),
    ],
    challenges: [
      ch('easy', 'abs()', 'abs(-7) into `n`.', pyVar('n', 'n == 7'), { starterCode: 'n = abs(-7)' }),
      ch('easy', 'round()', 'round(4.6) into `r`.', pyVar('r', 'r == 5'), { starterCode: 'r = round(4.6)' }),
      ch('medium', 'min max', 'max(1,9,3) into `m`.', pyVar('m', 'm == 9'), { starterCode: 'm = max(1, 9, 3)' }),
      ch('medium', 'pow()', 'pow(2, 5) into `p`.', pyVar('p', 'p == 32'), { starterCode: 'p = pow(2, 5)' }),
      ch('hard', 'math.sqrt', 'import math; sqrt(25) into `r`.', pyVar('r', 'r == 5.0'), { starterCode: 'import math\nr = math.sqrt(25)' }),
      ch('hard', 'divmod', 'divmod(17, 5) into `q, rem`.', pyVar('q', 'q == 3 and rem == 2', 'q, rem = divmod(17, 5)'), { starterCode: '' }),
      ch('expert', 'sum list', 'sum([1,2,3,4]) into `s`.', pyVar('s', 's == 10'), { starterCode: 's = sum([1,2,3,4])' }),
      ch('expert', 'float precision awareness', '0.1 + 0.2 into `x`; check not exactly 0.3.', pyVar('x', 'x != 0.3 and abs(x - 0.3) < 1e-10'), { starterCode: 'x = 0.1 + 0.2' }),
      ch('expert', 'math.floor', 'math.floor(3.9) into `n`.', pyVar('n', 'n == 3'), { starterCode: 'import math\nn = math.floor(3.9)' }),
      ch('expert', 'complex?', 'Assign `z = 2 + 3j`; type is complex.', pyVar('z', 'type(z) == complex'), { starterCode: 'z = ' }),
    ],
    questions: [
      qMcq('abs(-4) is:', [{ id: 'a', label: '4' }, { id: 'b', label: '-4' }, { id: 'c', label: '0' }], 'a'),
      qShort('Integer division operator?', '//'),
      qMcq('math.sqrt(9) is:', [{ id: 'a', label: '3.0' }, { id: 'b', label: '3 only int' }, { id: 'c', label: '81' }], 'a'),
      qShort('Function returning quotient and remainder?', 'divmod'),
      qMcq('0.1 + 0.2 == 0.3 is often:', [{ id: 'a', label: 'False due to float precision' }, { id: 'b', label: 'Always True' }, { id: 'c', label: 'Syntax error' }], 'a'),
      qMcq('pow(2,3) equals:', [{ id: 'a', label: '8' }, { id: 'b', label: '6' }, { id: 'c', label: '9' }], 'a'),
      qShort('sum([1,2]) equals?', '3'),
      qMcq('Expert: 2 + 3j is type:', [{ id: 'a', label: 'complex' }, { id: 'b', label: 'float' }, { id: 'c', label: 'str' }], 'a'),
      qMcq('Expert: round(2.5) in Python 3 rounds to:', [{ id: 'a', label: 'Even (banker\'s) in some cases' }, { id: 'b', label: 'Always up' }, { id: 'c', label: 'Always down' }], 'a'),
      qShort('Expert: Module for advanced math functions?', 'math'),
    ],
  }),
]
